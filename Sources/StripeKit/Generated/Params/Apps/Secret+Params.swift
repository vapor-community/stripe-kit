//
//  Secret+Params.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

public struct SecretCreateParams: StripeParams {
    /// A name for the secret that's unique within the scope.
    public var name: String
    /// The plaintext secret value to be stored.
    public var payload: String
    /// Specifies the scoping of the secret. Requests originating from UI extensions can only access
    /// account-scoped secrets or secrets scoped to their own user.
    public var scope: SecretCreateScope
    /// Specifies which fields in the response should be expanded.
    public var expand: [String]?
    /// The Unix timestamp for the expiry time of the secret, after which the secret deletes.
    public var expiresAt: Int?

    public init(name: String, payload: String, scope: SecretCreateScope, expand: [String]? = nil, expiresAt: Int? = nil) {
        self.name = name
        self.payload = payload
        self.scope = scope
        self.expand = expand
        self.expiresAt = expiresAt
    }

    public enum CodingKeys: String, CodingKey {
        case expand
        case expiresAt = "expires_at"
        case name
        case payload
        case scope
    }
}

public struct SecretDeleteParams: StripeParams {
    /// A name for the secret that's unique within the scope.
    public var name: String
    /// Specifies the scoping of the secret. Requests originating from UI extensions can only access
    /// account-scoped secrets or secrets scoped to their own user.
    public var scope: SecretDeleteScope
    /// Specifies which fields in the response should be expanded.
    public var expand: [String]?

    public init(name: String, scope: SecretDeleteScope, expand: [String]? = nil) {
        self.name = name
        self.scope = scope
        self.expand = expand
    }
}

/// Specifies the scoping of the secret. Requests originating from UI extensions can only access
/// account-scoped secrets or secrets scoped to their own user.
public struct SecretCreateScope: StripeParams {
    /// The secret scope type.
    public var type: SecretCreateScopeType
    /// The user ID. This field is required if `type` is set to `user`, and should not be provided if `type`
    /// is set to `account`.
    public var user: String?

    public init(type: SecretCreateScopeType, user: String? = nil) {
        self.type = type
        self.user = user
    }
}

/// Specifies the scoping of the secret. Requests originating from UI extensions can only access
/// account-scoped secrets or secrets scoped to their own user.
public struct SecretDeleteScope: StripeParams {
    /// The secret scope type.
    public var type: SecretDeleteScopeType
    /// The user ID. This field is required if `type` is set to `user`, and should not be provided if `type`
    /// is set to `account`.
    public var user: String?

    public init(type: SecretDeleteScopeType, user: String? = nil) {
        self.type = type
        self.user = user
    }
}

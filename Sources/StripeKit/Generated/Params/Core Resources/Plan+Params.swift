//
//  Plan+Params.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

public struct PlanCreateParams: StripeParams {
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// Specifies billing frequency. Either `day`, `week`, `month` or `year`.
    public var interval: PlanCreateInterval
    /// Whether the plan is currently available for new subscriptions. Defaults to `true`.
    public var active: Bool?
    /// A positive integer in cents (or local equivalent) (or 0 for a free plan) representing how much to
    /// charge on a recurring basis.
    public var amount: Int?
    /// Same as `amount`, but accepts a decimal value with at most 12 decimal places. Only one of `amount`
    /// and `amount_decimal` can be set.
    public var amountDecimal: String?
    /// Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates
    /// that the fixed amount (specified in `amount`) will be charged per unit in `quantity` (for plans with
    /// `usage_type=licensed`), or per unit of total usage (for plans with `usage_type=metered`). `tiered`
    /// indicates that the unit pricing will be computed using a tiering strategy as defined using the
    /// `tiers` and `tiers_mode` attributes.
    public var billingScheme: PlanCreateBillingScheme?
    /// Specifies which fields in the response should be expanded.
    public var expand: [String]?
    /// An identifier randomly generated by Stripe. Used to identify this plan when subscribing a customer.
    /// You can optionally override this ID, but the ID must be unique across all plans in your Stripe
    /// account. You can, however, use the same plan ID in both live and test modes.
    public var id: String?
    /// The number of intervals between subscription billings. For example, `interval=month` and
    /// `interval_count=3` bills every 3 months. Maximum of three years interval allowed (3 years, 36
    /// months, or 156 weeks).
    public var intervalCount: Int?
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    /// Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an
    /// empty value to `metadata`.
    public var metadata: [String: String]?
    /// The meter tracking the usage of a metered price
    public var meter: String?
    /// A brief description of the plan, hidden from customers.
    public var nickname: String?
    public var product: String?
    /// Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to
    /// `tiered`. See also the documentation for `billing_scheme`.
    public var tiers: [PlanCreateTiers]?
    /// Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the
    /// maximum quantity within a period determines the per unit price, in `graduated` tiering pricing can
    /// successively change as the quantity grows.
    public var tiersMode: PlanCreateTiersMode?
    /// Apply a transformation to the reported usage or set quantity before computing the billed price.
    /// Cannot be combined with `tiers`.
    public var transformUsage: PlanCreateTransformUsage?
    /// Default number of trial days when subscribing a customer to this plan using
    /// [`trial_from_plan=true`](https://docs.stripe.com/api#create_subscription-trial_from_plan).
    public var trialPeriodDays: Int?
    /// Configures how the quantity per period should be determined. Can be either `metered` or `licensed`.
    /// `licensed` automatically bills the `quantity` set when adding it to a subscription. `metered`
    /// aggregates the total usage based on usage records. Defaults to `licensed`.
    public var usageType: PlanCreateUsageType?

    public init(currency: String, interval: PlanCreateInterval, active: Bool? = nil, amount: Int? = nil, amountDecimal: String? = nil, billingScheme: PlanCreateBillingScheme? = nil, expand: [String]? = nil, id: String? = nil, intervalCount: Int? = nil, metadata: [String: String]? = nil, meter: String? = nil, nickname: String? = nil, product: String? = nil, tiers: [PlanCreateTiers]? = nil, tiersMode: PlanCreateTiersMode? = nil, transformUsage: PlanCreateTransformUsage? = nil, trialPeriodDays: Int? = nil, usageType: PlanCreateUsageType? = nil) {
        self.currency = currency
        self.interval = interval
        self.active = active
        self.amount = amount
        self.amountDecimal = amountDecimal
        self.billingScheme = billingScheme
        self.expand = expand
        self.id = id
        self.intervalCount = intervalCount
        self.metadata = metadata
        self.meter = meter
        self.nickname = nickname
        self.product = product
        self.tiers = tiers
        self.tiersMode = tiersMode
        self.transformUsage = transformUsage
        self.trialPeriodDays = trialPeriodDays
        self.usageType = usageType
    }

    public enum CodingKeys: String, CodingKey {
        case active
        case amount
        case amountDecimal = "amount_decimal"
        case billingScheme = "billing_scheme"
        case currency
        case expand
        case id
        case interval
        case intervalCount = "interval_count"
        case metadata
        case meter
        case nickname
        case product
        case tiers
        case tiersMode = "tiers_mode"
        case transformUsage = "transform_usage"
        case trialPeriodDays = "trial_period_days"
        case usageType = "usage_type"
    }
}

public struct PlanUpdateParams: StripeParams {
    /// Whether the plan is currently available for new subscriptions.
    public var active: Bool?
    /// Specifies which fields in the response should be expanded.
    public var expand: [String]?
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    /// Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an
    /// empty value to `metadata`.
    public var metadata: [String: String]?
    /// A brief description of the plan, hidden from customers.
    public var nickname: String?
    /// The product the plan belongs to. This cannot be changed once it has been used in a subscription or
    /// subscription schedule.
    public var product: String?
    /// Default number of trial days when subscribing a customer to this plan using
    /// [`trial_from_plan=true`](https://docs.stripe.com/api#create_subscription-trial_from_plan).
    public var trialPeriodDays: Int?

    public init(active: Bool? = nil, expand: [String]? = nil, metadata: [String: String]? = nil, nickname: String? = nil, product: String? = nil, trialPeriodDays: Int? = nil) {
        self.active = active
        self.expand = expand
        self.metadata = metadata
        self.nickname = nickname
        self.product = product
        self.trialPeriodDays = trialPeriodDays
    }

    public enum CodingKeys: String, CodingKey {
        case active
        case expand
        case metadata
        case nickname
        case product
        case trialPeriodDays = "trial_period_days"
    }
}

public struct PlanCreateTiers: StripeParams {
    /// Specifies the upper bound of this tier. The lower bound of a tier is the upper bound of the previous
    /// tier adding one. Use `inf` to define a fallback tier.
    public var upTo: String
    /// The flat billing amount for an entire tier, regardless of the number of units in the tier.
    public var flatAmount: Int?
    /// Same as `flat_amount`, but accepts a decimal value representing an integer in the minor units of the
    /// currency. Only one of `flat_amount` and `flat_amount_decimal` can be set.
    public var flatAmountDecimal: String?
    /// The per unit billing amount for each individual unit for which this tier applies.
    public var unitAmount: Int?
    /// Same as `unit_amount`, but accepts a decimal value in cents (or local equivalent) with at most 12
    /// decimal places. Only one of `unit_amount` and `unit_amount_decimal` can be set.
    public var unitAmountDecimal: String?

    public init(upTo: String, flatAmount: Int? = nil, flatAmountDecimal: String? = nil, unitAmount: Int? = nil, unitAmountDecimal: String? = nil) {
        self.upTo = upTo
        self.flatAmount = flatAmount
        self.flatAmountDecimal = flatAmountDecimal
        self.unitAmount = unitAmount
        self.unitAmountDecimal = unitAmountDecimal
    }

    public enum CodingKeys: String, CodingKey {
        case flatAmount = "flat_amount"
        case flatAmountDecimal = "flat_amount_decimal"
        case unitAmount = "unit_amount"
        case unitAmountDecimal = "unit_amount_decimal"
        case upTo = "up_to"
    }
}

/// Apply a transformation to the reported usage or set quantity before computing the billed price.
/// Cannot be combined with `tiers`.
public struct PlanCreateTransformUsage: StripeParams {
    /// Divide usage by this number.
    public var divideBy: Int
    /// After division, either round the result `up` or `down`.
    public var round: PlanCreateTransformUsageRound

    public init(divideBy: Int, round: PlanCreateTransformUsageRound) {
        self.divideBy = divideBy
        self.round = round
    }

    public enum CodingKeys: String, CodingKey {
        case divideBy = "divide_by"
        case round
    }
}

//
//  TaxTransaction+Params.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

public struct TaxTransactionCreateParams: StripeParams {
    /// Tax Calculation ID to be used as input when creating the transaction.
    public var calculation: String
    /// A custom order or sale identifier, such as 'myOrder_123'. Must be unique across all transactions,
    /// including reversals.
    public var reference: String
    /// Specifies which fields in the response should be expanded.
    public var expand: [String]?
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    /// Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an
    /// empty value to `metadata`.
    public var metadata: [String: String]?
    /// The Unix timestamp representing when the tax liability is assumed or reduced, which determines the
    /// liability posting period and handling in tax liability reports. The timestamp must fall within the
    /// `tax_date` and the current time, unless the `tax_date` is scheduled in advance. Defaults to the
    /// current time.
    public var postedAt: Int?

    public init(calculation: String, reference: String, expand: [String]? = nil, metadata: [String: String]? = nil, postedAt: Int? = nil) {
        self.calculation = calculation
        self.reference = reference
        self.expand = expand
        self.metadata = metadata
        self.postedAt = postedAt
    }

    public enum CodingKeys: String, CodingKey {
        case calculation
        case expand
        case metadata
        case postedAt = "posted_at"
        case reference
    }
}

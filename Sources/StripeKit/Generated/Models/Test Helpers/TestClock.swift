//
//  TestClock.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// A test clock enables deterministic control over objects in testmode. With a test clock, you can
/// create objects at a frozen time in the past or future, and advance to a specific future time to
/// observe webhooks and state changes. After the clock advances, you can either validate the current
/// state of your scenario (and test your assumptions), change the current state of your scenario (and
/// test more complex scenarios), or keep advancing forward in time.
public struct TestClock: Codable, Sendable {
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Time at which this clock is scheduled to auto delete.
    public var deletesAfter: Int
    /// Time at which all objects belonging to this clock are frozen.
    public var frozenTime: Int
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// The custom name supplied at creation.
    public var name: String?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: TestClockObject
    /// The status of the Test Clock.
    public var status: TestClockStatus
    @Expandable<BillingClocksResourceStatusDetailsStatusDetails>
    public var statusDetails: String?

    public enum CodingKeys: String, CodingKey {
        case created
        case deletesAfter = "deletes_after"
        case frozenTime = "frozen_time"
        case id
        case livemode
        case name
        case object
        case status
        case statusDetails = "status_details"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum TestClockObject: String, Codable, Sendable {
    case testHelpersTestClock = "test_helpers.test_clock"
}

/// The status of the Test Clock.
public enum TestClockStatus: String, Codable, Sendable {
    case advancing
    case internalFailure = "internal_failure"
    case ready
}

public struct TestClockList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [TestClock]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

//
//  Authorization.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// When an [issued card](https://docs.stripe.com/issuing) is used to make a purchase, an Issuing
/// `Authorization` object is created.
/// [Authorizations](https://docs.stripe.com/issuing/purchases/authorizations) must be approved for the
/// purchase to be completed successfully. Related guide: [Issued card
/// authorizations](https://docs.stripe.com/issuing/purchases/authorizations)
public struct Authorization: Codable, Sendable {
    /// The total amount that was authorized or rejected. This amount is in `currency` and in the [smallest
    /// currency unit](https://stripe.com/docs/currencies#zero-decimal). `amount` should be the same as
    /// `merchant_amount`, unless `currency` and `merchant_currency` are different.
    public var amount: Int
    /// Detailed breakdown of amount components. These amounts are denominated in `currency` and in the
    /// [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
    @Expandable<IssuingAuthorizationAmountDetails>
    public var amountDetails: String?
    /// Whether the authorization has been approved.
    public var approved: Bool
    /// How the card details were provided.
    public var authorizationMethod: AuthorizationAuthorizationMethod
    /// List of balance transactions associated with this authorization.
    @ExpandableCollection<BalanceTransaction>
    public var balanceTransactions: [String]?
    @Expandable<IssuingCard>
    public var card: String?
    /// The cardholder to whom this authorization belongs.
    @Expandable<Cardholder>
    public var cardholder: String?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// The currency of the cardholder. This currency can be different from the currency presented at
    /// authorization and the `merchant_currency` field on this authorization. Three-letter [ISO currency
    /// code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported
    /// currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// Fleet-specific information for authorizations using Fleet cards.
    @Expandable<IssuingAuthorizationFleetData>
    public var fleet: String?
    /// Fraud challenges sent to the cardholder, if this authorization was declined for fraud risk reasons.
    @ExpandableCollection<IssuingAuthorizationFraudChallenge>
    public var fraudChallenges: [String]?
    /// Information about fuel that was purchased with this transaction. Typically this information is
    /// received from the merchant after the authorization has been approved and the fuel dispensed.
    @Expandable<IssuingAuthorizationFuelData>
    public var fuel: String?
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// The total amount that was authorized or rejected. This amount is in the `merchant_currency` and in
    /// the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal). `merchant_amount`
    /// should be the same as `amount`, unless `merchant_currency` and `currency` are different.
    public var merchantAmount: Int
    /// The local currency that was presented to the cardholder for the authorization. This currency can be
    /// different from the cardholder currency and the `currency` field on this authorization. Three-letter
    /// [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a
    /// [supported currency](https://stripe.com/docs/currencies).
    public var merchantCurrency: String
    @Expandable<IssuingAuthorizationMerchantData>
    public var merchantData: String?
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]
    /// Details about the authorization, such as identifiers, set by the card network.
    @Expandable<IssuingAuthorizationNetworkData>
    public var networkData: String?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: AuthorizationObject
    /// The pending authorization request. This field will only be non-null during an
    /// `issuing_authorization.request` webhook.
    @Expandable<IssuingAuthorizationPendingRequest>
    public var pendingRequest: String?
    /// History of every time a `pending_request` authorization was approved/declined, either by you
    /// directly or by Stripe (e.g. based on your spending_controls). If the merchant changes the
    /// authorization by performing an incremental authorization, you can look at this field to see the
    /// previous requests for the authorization. This field can be helpful in determining why a given
    /// authorization was approved/declined.
    @ExpandableCollection<IssuingAuthorizationRequest>
    public var requestHistory: [String]?
    /// The current status of the authorization in its lifecycle.
    public var status: AuthorizationStatus
    /// [Token](https://docs.stripe.com/api/issuing/tokens/object) object used for this authorization. If a
    /// network token was not used for this authorization, this field will be null.
    @Expandable<IssuingToken>
    public var token: String?
    /// List of [transactions](https://docs.stripe.com/api/issuing/transactions) associated with this
    /// authorization.
    @ExpandableCollection<IssuingTransaction>
    public var transactions: [String]?
    /// [Treasury](https://docs.stripe.com/api/treasury) details related to this authorization if it was
    /// created on a [FinancialAccount](https://docs.stripe.com/api/treasury/financial_accounts).
    @Expandable<IssuingAuthorizationTreasury>
    public var treasury: String?
    @Expandable<IssuingAuthorizationVerificationData>
    public var verificationData: String?
    /// Whether the authorization bypassed fraud risk checks because the cardholder has previously completed
    /// a fraud challenge on a similar high-risk authorization from the same merchant.
    public var verifiedByFraudChallenge: Bool?
    /// The digital wallet used for this transaction. One of `apple_pay`, `google_pay`, or `samsung_pay`.
    /// Will populate as `null` when no digital wallet was utilized.
    public var wallet: String?

    public enum CodingKeys: String, CodingKey {
        case amount
        case amountDetails = "amount_details"
        case approved
        case authorizationMethod = "authorization_method"
        case balanceTransactions = "balance_transactions"
        case card
        case cardholder
        case created
        case currency
        case fleet
        case fraudChallenges = "fraud_challenges"
        case fuel
        case id
        case livemode
        case merchantAmount = "merchant_amount"
        case merchantCurrency = "merchant_currency"
        case merchantData = "merchant_data"
        case metadata
        case networkData = "network_data"
        case object
        case pendingRequest = "pending_request"
        case requestHistory = "request_history"
        case status
        case token
        case transactions
        case treasury
        case verificationData = "verification_data"
        case verifiedByFraudChallenge = "verified_by_fraud_challenge"
        case wallet
    }
}

/// How the card details were provided.
public enum AuthorizationAuthorizationMethod: String, Codable, Sendable {
    case chip
    case contactless
    case keyedIn = "keyed_in"
    case online
    case swipe
}

/// String representing the object's type. Objects of the same type share the same value.
public enum AuthorizationObject: String, Codable, Sendable {
    case issuingAuthorization = "issuing.authorization"
}

/// The current status of the authorization in its lifecycle.
public enum AuthorizationStatus: String, Codable, Sendable {
    case closed
    case expired
    case pending
    case reversed
}

public struct AuthorizationList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Authorization]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

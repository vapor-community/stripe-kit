//
//  Registration.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// A Tax `Registration` lets us know that your business is registered to collect tax on payments within
/// a region, enabling you to [automatically collect tax](https://docs.stripe.com/tax). Stripe doesn't
/// register on your behalf with the relevant authorities when you create a Tax `Registration` object.
/// For more information on how to register to collect tax, see [our
/// guide](https://docs.stripe.com/tax/registering). Related guide: [Using the Registrations
/// API](https://docs.stripe.com/tax/registrations-api)
public struct Registration: Codable, Sendable {
    /// Time at which the registration becomes active. Measured in seconds since the Unix epoch.
    public var activeFrom: Int
    /// Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
    public var country: String
    @Expandable<TaxProductRegistrationsResourceCountryOptions>
    public var countryOptions: String?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// If set, the registration stops being active at this time. If not set, the registration will be
    /// active indefinitely. Measured in seconds since the Unix epoch.
    public var expiresAt: Int?
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: RegistrationObject
    /// The status of the registration. This field is present for convenience and can be deduced from
    /// `active_from` and `expires_at`.
    public var status: RegistrationStatus

    public enum CodingKeys: String, CodingKey {
        case activeFrom = "active_from"
        case country
        case countryOptions = "country_options"
        case created
        case expiresAt = "expires_at"
        case id
        case livemode
        case object
        case status
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum RegistrationObject: String, Codable, Sendable {
    case taxRegistration = "tax.registration"
}

/// The status of the registration. This field is present for convenience and can be deduced from
/// `active_from` and `expires_at`.
public enum RegistrationStatus: String, Codable, Sendable {
    case active
    case expired
    case scheduled
}

public struct RegistrationList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Registration]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

//
//  CalculationLineItem.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

public struct CalculationLineItem: Codable, Sendable {
    /// The line item amount in the [smallest currency
    /// unit](https://docs.stripe.com/currencies#zero-decimal). If `tax_behavior=inclusive`, then this
    /// amount includes taxes. Otherwise, taxes were calculated on top of this amount.
    public var amount: Int
    /// The amount of tax calculated for this line item, in the [smallest currency
    /// unit](https://docs.stripe.com/currencies#zero-decimal).
    public var amountTax: Int
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: CalculationLineItemObject
    /// The ID of an existing [Product](https://docs.stripe.com/api/products/object).
    public var product: String?
    /// The number of units of the item being purchased. For reversals, this is the quantity reversed.
    public var quantity: Int
    /// A custom identifier for this line item.
    public var reference: String
    /// Specifies whether the `amount` includes taxes. If `tax_behavior=inclusive`, then the amount includes
    /// taxes.
    public var taxBehavior: CalculationLineItemTaxBehavior
    /// Detailed account of taxes relevant to this line item.
    @ExpandableCollection<TaxProductResourceLineItemTaxBreakdown>
    public var taxBreakdown: [String]?
    /// The [tax code](https://docs.stripe.com/tax/tax-categories) ID used for this resource.
    public var taxCode: String

    public enum CodingKeys: String, CodingKey {
        case amount
        case amountTax = "amount_tax"
        case id
        case livemode
        case metadata
        case object
        case product
        case quantity
        case reference
        case taxBehavior = "tax_behavior"
        case taxBreakdown = "tax_breakdown"
        case taxCode = "tax_code"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum CalculationLineItemObject: String, Codable, Sendable {
    case taxCalculationLineItem = "tax.calculation_line_item"
}

/// Specifies whether the `amount` includes taxes. If `tax_behavior=inclusive`, then the amount includes
/// taxes.
public enum CalculationLineItemTaxBehavior: String, Codable, Sendable {
    case exclusive
    case inclusive
}

public struct CalculationLineItemList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [CalculationLineItem]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

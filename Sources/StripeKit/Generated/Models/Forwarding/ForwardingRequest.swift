//
//  ForwardingRequest.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// Instructs Stripe to make a request on your behalf using the destination URL. The destination URL is
/// activated by Stripe at the time of onboarding. Stripe verifies requests with your credentials
/// provided during onboarding, and injects card details from the payment_method into the request.
/// Stripe redacts all sensitive fields and headers, including authentication credentials and card
/// numbers, before storing the request and response data in the forwarding Request object, which are
/// subject to a 30-day retention period. You can provide a Stripe idempotency key to make sure that
/// requests with the same key result in only one outbound request. The Stripe idempotency key provided
/// should be unique and different from any idempotency keys provided on the underlying third-party
/// request. Forwarding Requests are synchronous requests that return a response or time out according
/// to Stripe’s limits. Related guide: [Forward card details to third-party API
/// endpoints](https://docs.stripe.com/payments/forwarding).
public struct ForwardingRequest: Codable, Sendable {
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: ForwardingRequestObject
    /// The PaymentMethod to insert into the forwarded request. Forwarding previously consumed
    /// PaymentMethods is allowed.
    public var paymentMethod: String
    /// The field kinds to be replaced in the forwarded request.
    public var replacements: [String]
    /// Context about the request from Stripe's servers to the destination endpoint.
    @Expandable<ForwardedRequestContext>
    public var requestContext: String?
    /// The request that was sent to the destination endpoint. We redact any sensitive fields.
    @Expandable<ForwardedRequestDetails>
    public var requestDetails: String?
    /// The response that the destination endpoint returned to us. We redact any sensitive fields.
    @Expandable<ForwardedResponseDetails>
    public var responseDetails: String?
    /// The destination URL for the forwarded request. Must be supported by the config.
    public var url: String?

    public enum CodingKeys: String, CodingKey {
        case created
        case id
        case livemode
        case metadata
        case object
        case paymentMethod = "payment_method"
        case replacements
        case requestContext = "request_context"
        case requestDetails = "request_details"
        case responseDetails = "response_details"
        case url
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum ForwardingRequestObject: String, Codable, Sendable {
    case forwardingRequest = "forwarding.request"
}

public struct ForwardingRequestList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [ForwardingRequest]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

//
//  InboundTransfer.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// Use
/// [InboundTransfers](https://docs.stripe.com/docs/treasury/moving-money/financial-accounts/into/inbound-transfers)
/// to add funds to your [FinancialAccount](https://api.stripe.com#financial_accounts) via a
/// PaymentMethod that is owned by you. The funds will be transferred via an ACH debit. Related guide:
/// [Moving money with Treasury using InboundTransfer
/// objects](https://docs.stripe.com/docs/treasury/moving-money/financial-accounts/into/inbound-transfers)
public struct InboundTransfer: Codable, Sendable {
    /// Amount (in cents) transferred.
    public var amount: Int
    /// Returns `true` if the InboundTransfer is able to be canceled.
    public var cancelable: Bool
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String?
    /// Details about this InboundTransfer's failure. Only set when status is `failed`.
    @Expandable<TreasuryInboundTransfersResourceFailureDetails>
    public var failureDetails: String?
    /// The FinancialAccount that received the funds.
    public var financialAccount: String
    /// A [hosted transaction receipt](https://docs.stripe.com/treasury/moving-money/regulatory-receipts)
    /// URL that is provided when money movement is considered regulated under Stripe's money transmission
    /// licenses.
    public var hostedRegulatoryReceiptUrl: String?
    /// Unique identifier for the object.
    public var id: String
    @Expandable<TreasuryInboundTransfersResourceInboundTransferResourceLinkedFlows>
    public var linkedFlows: String?
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: InboundTransferObject
    /// The origin payment method to be debited for an InboundTransfer.
    public var originPaymentMethod: String?
    /// Details about the PaymentMethod for an InboundTransfer.
    @Expandable<InboundTransfers>
    public var originPaymentMethodDetails: String?
    /// Returns `true` if the funds for an InboundTransfer were returned after the InboundTransfer went to
    /// the `succeeded` state.
    public var returned: Bool?
    /// Statement descriptor shown when funds are debited from the source. Not all payment networks support
    /// `statement_descriptor`.
    public var statementDescriptor: String
    /// Status of the InboundTransfer: `processing`, `succeeded`, `failed`, and `canceled`. An
    /// InboundTransfer is `processing` if it is created and pending. The status changes to `succeeded` once
    /// the funds have been "confirmed" and a `transaction` is created and posted. The status changes to
    /// `failed` if the transfer fails.
    public var status: InboundTransferStatus
    @Expandable<TreasuryInboundTransfersResourceInboundTransferResourceStatusTransitions>
    public var statusTransitions: String?
    /// The Transaction associated with this object.
    @Expandable<TreasuryTransaction>
    public var transaction: String?

    public enum CodingKeys: String, CodingKey {
        case amount
        case cancelable
        case created
        case currency
        case description
        case failureDetails = "failure_details"
        case financialAccount = "financial_account"
        case hostedRegulatoryReceiptUrl = "hosted_regulatory_receipt_url"
        case id
        case linkedFlows = "linked_flows"
        case livemode
        case metadata
        case object
        case originPaymentMethod = "origin_payment_method"
        case originPaymentMethodDetails = "origin_payment_method_details"
        case returned
        case statementDescriptor = "statement_descriptor"
        case status
        case statusTransitions = "status_transitions"
        case transaction
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum InboundTransferObject: String, Codable, Sendable {
    case treasuryInboundTransfer = "treasury.inbound_transfer"
}

/// Status of the InboundTransfer: `processing`, `succeeded`, `failed`, and `canceled`. An
/// InboundTransfer is `processing` if it is created and pending. The status changes to `succeeded` once
/// the funds have been "confirmed" and a `transaction` is created and posted. The status changes to
/// `failed` if the transfer fails.
public enum InboundTransferStatus: String, Codable, Sendable {
    case canceled
    case failed
    case processing
    case succeeded
}

public struct InboundTransferList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [InboundTransfer]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

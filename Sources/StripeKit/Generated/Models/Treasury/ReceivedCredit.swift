//
//  ReceivedCredit.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// ReceivedCredits represent funds sent to a
/// [FinancialAccount](https://api.stripe.com#financial_accounts) (for example, via ACH or wire). These
/// money movements are not initiated from the FinancialAccount.
public struct ReceivedCredit: Codable, Sendable {
    /// Amount (in cents) transferred.
    public var amount: Int
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String
    /// Reason for the failure. A ReceivedCredit might fail because the receiving FinancialAccount is closed
    /// or frozen.
    public var failureCode: ReceivedCreditFailureCode?
    /// The FinancialAccount that received the funds.
    public var financialAccount: String?
    /// A [hosted transaction receipt](https://docs.stripe.com/treasury/moving-money/regulatory-receipts)
    /// URL that is provided when money movement is considered regulated under Stripe's money transmission
    /// licenses.
    public var hostedRegulatoryReceiptUrl: String?
    /// Unique identifier for the object.
    public var id: String
    @Expandable<TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetails>
    public var initiatingPaymentMethodDetails: String?
    @Expandable<TreasuryReceivedCreditsResourceLinkedFlows>
    public var linkedFlows: String?
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// The rails used to send the funds.
    public var network: ReceivedCreditNetwork
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: ReceivedCreditObject
    /// Details describing when a ReceivedCredit may be reversed.
    @Expandable<TreasuryReceivedCreditsResourceReversalDetails>
    public var reversalDetails: String?
    /// Status of the ReceivedCredit. ReceivedCredits are created either `succeeded` (approved) or `failed`
    /// (declined). If a ReceivedCredit is declined, the failure reason can be found in the `failure_code`
    /// field.
    public var status: ReceivedCreditStatus
    /// The Transaction associated with this object.
    @Expandable<TreasuryTransaction>
    public var transaction: String?

    public enum CodingKeys: String, CodingKey {
        case amount
        case created
        case currency
        case description
        case failureCode = "failure_code"
        case financialAccount = "financial_account"
        case hostedRegulatoryReceiptUrl = "hosted_regulatory_receipt_url"
        case id
        case initiatingPaymentMethodDetails = "initiating_payment_method_details"
        case linkedFlows = "linked_flows"
        case livemode
        case network
        case object
        case reversalDetails = "reversal_details"
        case status
        case transaction
    }
}

/// Reason for the failure. A ReceivedCredit might fail because the receiving FinancialAccount is closed
/// or frozen.
public enum ReceivedCreditFailureCode: String, Codable, Sendable {
    case accountClosed = "account_closed"
    case accountFrozen = "account_frozen"
    case internationalTransaction = "international_transaction"
    case other
}

/// The rails used to send the funds.
public enum ReceivedCreditNetwork: String, Codable, Sendable {
    case ach
    case card
    case stripe
    case usDomesticWire = "us_domestic_wire"
}

/// String representing the object's type. Objects of the same type share the same value.
public enum ReceivedCreditObject: String, Codable, Sendable {
    case treasuryReceivedCredit = "treasury.received_credit"
}

/// Status of the ReceivedCredit. ReceivedCredits are created either `succeeded` (approved) or `failed`
/// (declined). If a ReceivedCredit is declined, the failure reason can be found in the `failure_code`
/// field.
public enum ReceivedCreditStatus: String, Codable, Sendable {
    case failed
    case succeeded
}

public struct ReceivedCreditList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [ReceivedCredit]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

//
//  MeterEventSummary.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// A billing meter event summary represents an aggregated view of a customer's billing meter events
/// within a specified timeframe. It indicates how much usage was accrued by a customer for that period.
/// Note: Meters events are aggregated asynchronously so the meter event summaries provide an eventually
/// consistent view of the reported usage.
public struct MeterEventSummary: Codable, Sendable {
    /// Aggregated value of all the events within `start_time` (inclusive) and `end_time` (inclusive). The
    /// aggregation strategy is defined on meter via `default_aggregation`.
    public var aggregatedValue: Double
    /// End timestamp for this event summary (exclusive). Must be aligned with minute boundaries.
    public var endTime: Int
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// The meter associated with this event summary.
    public var meter: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: MeterEventSummaryObject
    /// Start timestamp for this event summary (inclusive). Must be aligned with minute boundaries.
    public var startTime: Int

    public enum CodingKeys: String, CodingKey {
        case aggregatedValue = "aggregated_value"
        case endTime = "end_time"
        case id
        case livemode
        case meter
        case object
        case startTime = "start_time"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum MeterEventSummaryObject: String, Codable, Sendable {
    case billingMeterEventSummary = "billing.meter_event_summary"
}

public struct MeterEventSummaryList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [MeterEventSummary]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

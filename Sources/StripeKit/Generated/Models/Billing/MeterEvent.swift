//
//  MeterEvent.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// Meter events represent actions that customers take in your system. You can use meter events to bill
/// a customer based on their usage. Meter events are associated with billing meters, which define both
/// the contents of the event’s payload and how to aggregate those events.
public struct MeterEvent: Codable, Sendable {
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// The name of the meter event. Corresponds with the `event_name` field on a meter.
    public var eventName: String
    /// A unique identifier for the event.
    public var identifier: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: MeterEventObject
    /// The payload of the event. This contains the fields corresponding to a meter's
    /// `customer_mapping.event_payload_key` (default is `stripe_customer_id`) and
    /// `value_settings.event_payload_key` (default is `value`). Read more about the
    /// [payload](https://docs.stripe.com/billing/subscriptions/usage-based/meters/configure#meter-configuration-attributes).
    public var payload: [String: String]
    /// The timestamp passed in when creating the event. Measured in seconds since the Unix epoch.
    public var timestamp: Int

    public enum CodingKeys: String, CodingKey {
        case created
        case eventName = "event_name"
        case identifier
        case livemode
        case object
        case payload
        case timestamp
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum MeterEventObject: String, Codable, Sendable {
    case billingMeterEvent = "billing.meter_event"
}

public struct MeterEventList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [MeterEvent]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

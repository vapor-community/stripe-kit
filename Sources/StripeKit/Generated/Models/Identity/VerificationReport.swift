//
//  VerificationReport.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// A VerificationReport is the result of an attempt to collect and verify data from a user. The
/// collection of verification checks performed is determined from the `type` and `options` parameters
/// used. You can find the result of each verification check performed in the appropriate sub-resource:
/// `document`, `id_number`, `selfie`. Each VerificationReport contains a copy of any data collected by
/// the user as well as reference IDs which can be used to access collected images through the
/// [FileUpload](https://docs.stripe.com/api/files) API. To configure and create VerificationReports,
/// use the [VerificationSession](https://docs.stripe.com/api/identity/verification_sessions) API.
/// Related guide: [Accessing verification
/// results](https://docs.stripe.com/identity/verification-sessions#results).
public struct VerificationReport: Codable, Sendable {
    /// A string to reference this user. This can be a customer ID, a session ID, or similar, and can be
    /// used to reconcile this verification with your internal systems.
    public var clientReferenceId: String?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    @Expandable<GelatoDocumentReport>
    public var document: String?
    @Expandable<GelatoEmailReport>
    public var email: String?
    /// Unique identifier for the object.
    public var id: String
    @Expandable<GelatoIdNumberReport>
    public var idNumber: String?
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: VerificationReportObject
    @Expandable<GelatoVerificationReportOptions>
    public var options: String?
    @Expandable<GelatoPhoneReport>
    public var phone: String?
    @Expandable<GelatoSelfieReport>
    public var selfie: String?
    /// Type of report.
    public var type: VerificationReportType
    /// The configuration token of a verification flow from the dashboard.
    public var verificationFlow: String?
    /// ID of the VerificationSession that created this report.
    public var verificationSession: String?

    public enum CodingKeys: String, CodingKey {
        case clientReferenceId = "client_reference_id"
        case created
        case document
        case email
        case id
        case idNumber = "id_number"
        case livemode
        case object
        case options
        case phone
        case selfie
        case type
        case verificationFlow = "verification_flow"
        case verificationSession = "verification_session"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum VerificationReportObject: String, Codable, Sendable {
    case identityVerificationReport = "identity.verification_report"
}

/// Type of report.
public enum VerificationReportType: String, Codable, Sendable {
    case document
    case idNumber = "id_number"
    case verificationFlow = "verification_flow"
}

public struct VerificationReportList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [VerificationReport]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

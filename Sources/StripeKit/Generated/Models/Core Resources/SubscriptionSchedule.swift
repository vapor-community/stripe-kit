//
//  SubscriptionSchedule.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// A subscription schedule allows you to create and manage the lifecycle of a subscription by
/// predefining expected changes. Related guide: [Subscription
/// schedules](https://docs.stripe.com/billing/subscriptions/subscription-schedules)
public struct SubscriptionSchedule: Codable, Sendable {
    /// ID of the Connect Application that created the schedule.
    @Expandable<Application>
    public var application: String?
    @Expandable<SubscriptionsResourceBillingMode>
    public var billingMode: String?
    /// Time at which the subscription schedule was canceled. Measured in seconds since the Unix epoch.
    public var canceledAt: Int?
    /// Time at which the subscription schedule was completed. Measured in seconds since the Unix epoch.
    public var completedAt: Int?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Object representing the start and end dates for the current phase of the subscription schedule, if
    /// it is `active`.
    @Expandable<SubscriptionScheduleCurrentPhase>
    public var currentPhase: String?
    /// ID of the customer who owns the subscription schedule.
    @Expandable<Customer>
    public var customer: String?
    /// ID of the account who owns the subscription schedule.
    public var customerAccount: String?
    @Expandable<SubscriptionSchedulesResourceDefaultSettings>
    public var defaultSettings: String?
    /// Behavior of the subscription schedule and underlying subscription when it ends. Possible values are
    /// `release` or `cancel` with the default being `release`. `release` will end the subscription schedule
    /// and keep the underlying subscription running. `cancel` will end the subscription schedule and cancel
    /// the underlying subscription.
    public var endBehavior: SubscriptionScheduleEndBehavior
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: SubscriptionScheduleObject
    /// Configuration for the subscription schedule's phases.
    @ExpandableCollection<SubscriptionSchedulePhaseConfiguration>
    public var phases: [String]?
    /// Time at which the subscription schedule was released. Measured in seconds since the Unix epoch.
    public var releasedAt: Int?
    /// ID of the subscription once managed by the subscription schedule (if it is released).
    public var releasedSubscription: String?
    /// The present status of the subscription schedule. Possible values are `not_started`, `active`,
    /// `completed`, `released`, and `canceled`. You can read more about the different states in our
    /// [behavior guide](https://docs.stripe.com/billing/subscriptions/subscription-schedules).
    public var status: SubscriptionScheduleStatus
    /// ID of the subscription managed by the subscription schedule.
    @Expandable<Subscription>
    public var subscription: String?
    /// ID of the test clock this subscription schedule belongs to.
    @Expandable<TestClock>
    public var testClock: String?

    public enum CodingKeys: String, CodingKey {
        case application
        case billingMode = "billing_mode"
        case canceledAt = "canceled_at"
        case completedAt = "completed_at"
        case created
        case currentPhase = "current_phase"
        case customer
        case customerAccount = "customer_account"
        case defaultSettings = "default_settings"
        case endBehavior = "end_behavior"
        case id
        case livemode
        case metadata
        case object
        case phases
        case releasedAt = "released_at"
        case releasedSubscription = "released_subscription"
        case status
        case subscription
        case testClock = "test_clock"
    }
}

/// Behavior of the subscription schedule and underlying subscription when it ends. Possible values are
/// `release` or `cancel` with the default being `release`. `release` will end the subscription schedule
/// and keep the underlying subscription running. `cancel` will end the subscription schedule and cancel
/// the underlying subscription.
public enum SubscriptionScheduleEndBehavior: String, Codable, Sendable {
    case cancel
    case none
    case release
    case renew
}

/// String representing the object's type. Objects of the same type share the same value.
public enum SubscriptionScheduleObject: String, Codable, Sendable {
    case subscriptionSchedule = "subscription_schedule"
}

/// The present status of the subscription schedule. Possible values are `not_started`, `active`,
/// `completed`, `released`, and `canceled`. You can read more about the different states in our
/// [behavior guide](https://docs.stripe.com/billing/subscriptions/subscription-schedules).
public enum SubscriptionScheduleStatus: String, Codable, Sendable {
    case active
    case canceled
    case completed
    case notStarted = "not_started"
    case released
}

public struct SubscriptionScheduleList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [SubscriptionSchedule]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

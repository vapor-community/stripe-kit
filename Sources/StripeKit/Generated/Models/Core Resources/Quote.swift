//
//  Quote.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// A Quote is a way to model prices that you'd like to provide to a customer. Once accepted, it will
/// automatically create an invoice, subscription or subscription schedule.
public struct Quote: Codable, Sendable {
    /// Total before any discounts or taxes are applied.
    public var amountSubtotal: Int
    /// Total after discounts and taxes are applied.
    public var amountTotal: Int
    /// ID of the Connect Application that created the quote.
    @Expandable<Application>
    public var application: String?
    /// The amount of the application fee (if any) that will be requested to be applied to the payment and
    /// transferred to the application owner's Stripe account. Only applicable if there are no line items
    /// with recurring prices on the quote.
    public var applicationFeeAmount: Int?
    /// A non-negative decimal between 0 and 100, with at most two decimal places. This represents the
    /// percentage of the subscription invoice total that will be transferred to the application owner's
    /// Stripe account. Only applicable if there are line items with recurring prices on the quote.
    public var applicationFeePercent: Double?
    @Expandable<QuotesResourceAutomaticTax>
    public var automaticTax: String?
    /// Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt
    /// to pay invoices at the end of the subscription cycle or on finalization using the default payment
    /// method attached to the subscription or customer. When sending an invoice, Stripe will email your
    /// customer an invoice with payment instructions and mark the subscription as `active`. Defaults to
    /// `charge_automatically`.
    public var collectionMethod: QuoteCollectionMethod
    @Expandable<QuotesResourceComputed>
    public var computed: String?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String?
    /// The customer who received this quote. A customer is required to finalize the quote. Once specified,
    /// you can't change it.
    @Expandable<Customer>
    public var customer: String?
    /// The account representing the customer who received this quote. A customer or account is required to
    /// finalize the quote. Once specified, you can't change it.
    public var customerAccount: String?
    /// The tax rates applied to this quote.
    @ExpandableCollection<TaxRate>
    public var defaultTaxRates: [String]?
    /// A description that will be displayed on the quote PDF.
    public var description: String?
    /// The discounts applied to this quote.
    @ExpandableCollection<Discount>
    public var discounts: [String]?
    /// The date on which the quote will be canceled if in `open` or `draft` status. Measured in seconds
    /// since the Unix epoch.
    public var expiresAt: Int
    /// A footer that will be displayed on the quote PDF.
    public var footer: String?
    /// Details of the quote that was cloned. See the [cloning
    /// documentation](https://docs.stripe.com/quotes/clone) for more details.
    @Expandable<QuotesResourceFromQuote>
    public var fromQuote: String?
    /// A header that will be displayed on the quote PDF.
    public var header: String?
    /// Unique identifier for the object.
    public var id: String
    /// The invoice that was created from this quote.
    @Expandable<Invoice>
    public var invoice: String?
    @Expandable<InvoiceSettingQuoteSetting>
    public var invoiceSettings: String?
    /// A list of items the customer is being quoted for.
    public var lineItems: ItemList?
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]
    /// A unique number that identifies this particular quote. This number is assigned once the quote is
    /// [finalized](https://docs.stripe.com/quotes/overview#finalize).
    public var number: String?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: QuoteObject
    /// The account on behalf of which to charge. See the [Connect
    /// documentation](https://support.stripe.com/questions/sending-invoices-on-behalf-of-connected-accounts)
    /// for details.
    @Expandable<Account>
    public var onBehalfOf: String?
    /// The status of the quote.
    public var status: QuoteStatus
    @Expandable<QuotesResourceStatusTransitions>
    public var statusTransitions: String?
    /// The subscription that was created or updated from this quote.
    @Expandable<Subscription>
    public var subscription: String?
    @Expandable<QuotesResourceSubscriptionDataSubscriptionData>
    public var subscriptionData: String?
    /// The subscription schedule that was created or updated from this quote.
    @Expandable<SubscriptionSchedule>
    public var subscriptionSchedule: String?
    /// ID of the test clock this quote belongs to.
    @Expandable<TestClock>
    public var testClock: String?
    @Expandable<QuotesResourceTotalDetails>
    public var totalDetails: String?
    /// The account (if any) the payments will be attributed to for tax reporting, and where funds from each
    /// payment will be transferred to for each of the invoices.
    @Expandable<QuotesResourceTransferData>
    public var transferData: String?

    public enum CodingKeys: String, CodingKey {
        case amountSubtotal = "amount_subtotal"
        case amountTotal = "amount_total"
        case application
        case applicationFeeAmount = "application_fee_amount"
        case applicationFeePercent = "application_fee_percent"
        case automaticTax = "automatic_tax"
        case collectionMethod = "collection_method"
        case computed
        case created
        case currency
        case customer
        case customerAccount = "customer_account"
        case defaultTaxRates = "default_tax_rates"
        case description
        case discounts
        case expiresAt = "expires_at"
        case footer
        case fromQuote = "from_quote"
        case header
        case id
        case invoice
        case invoiceSettings = "invoice_settings"
        case lineItems = "line_items"
        case livemode
        case metadata
        case number
        case object
        case onBehalfOf = "on_behalf_of"
        case status
        case statusTransitions = "status_transitions"
        case subscription
        case subscriptionData = "subscription_data"
        case subscriptionSchedule = "subscription_schedule"
        case testClock = "test_clock"
        case totalDetails = "total_details"
        case transferData = "transfer_data"
    }
}

/// Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt
/// to pay invoices at the end of the subscription cycle or on finalization using the default payment
/// method attached to the subscription or customer. When sending an invoice, Stripe will email your
/// customer an invoice with payment instructions and mark the subscription as `active`. Defaults to
/// `charge_automatically`.
public enum QuoteCollectionMethod: String, Codable, Sendable {
    case chargeAutomatically = "charge_automatically"
    case sendInvoice = "send_invoice"
}

/// String representing the object's type. Objects of the same type share the same value.
public enum QuoteObject: String, Codable, Sendable {
    case quote
}

/// The status of the quote.
public enum QuoteStatus: String, Codable, Sendable {
    case accepted
    case canceled
    case draft
    case `open` = "open"
}

public struct QuoteList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Quote]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

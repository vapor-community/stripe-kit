//
//  PromotionCode.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// A Promotion Code represents a customer-redeemable code for an underlying promotion. You can create
/// multiple codes for a single promotion. If you enable promotion codes in your [customer portal
/// configuration](https://docs.stripe.com/customer-management/configure-portal), then customers can
/// redeem a code themselves when updating a subscription in the portal. Customers can also view the
/// currently active promotion codes and coupons on each of their subscriptions in the portal.
public struct PromotionCode: Codable, Sendable {
    /// Whether the promotion code is currently active. A promotion code is only active if the coupon is
    /// also valid.
    public var active: Bool
    /// The customer-facing code. Regardless of case, this code must be unique across all active promotion
    /// codes for each customer. Valid characters are lower case letters (a-z), upper case letters (A-Z),
    /// and digits (0-9).
    public var code: String
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// The customer who can use this promotion code.
    @Expandable<Customer>
    public var customer: String?
    /// The account representing the customer who can use this promotion code.
    public var customerAccount: String?
    /// Date at which the promotion code can no longer be redeemed.
    public var expiresAt: Int?
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Maximum number of times this promotion code can be redeemed.
    public var maxRedemptions: Int?
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: PromotionCodeObject
    @Expandable<PromotionCodesResourcePromotion>
    public var promotion: String?
    @Expandable<PromotionCodesResourceRestrictions>
    public var restrictions: String?
    /// Number of times this promotion code has been used.
    public var timesRedeemed: Int

    public enum CodingKeys: String, CodingKey {
        case active
        case code
        case created
        case customer
        case customerAccount = "customer_account"
        case expiresAt = "expires_at"
        case id
        case livemode
        case maxRedemptions = "max_redemptions"
        case metadata
        case object
        case promotion
        case restrictions
        case timesRedeemed = "times_redeemed"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum PromotionCodeObject: String, Codable, Sendable {
    case promotionCode = "promotion_code"
}

public struct PromotionCodeList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [PromotionCode]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

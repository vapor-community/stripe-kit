//
//  Topup.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// To top up your Stripe balance, you create a top-up object. You can retrieve individual top-ups, as
/// well as list all top-ups. Top-ups are identified by a unique, random ID. Related guide: [Topping up
/// your platform account](https://docs.stripe.com/connect/top-ups)
public struct Topup: Codable, Sendable {
    /// Amount transferred.
    public var amount: Int
    /// ID of the balance transaction that describes the impact of this top-up on your account balance. May
    /// not be specified depending on status of top-up.
    @Expandable<BalanceTransaction>
    public var balanceTransaction: String?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String?
    /// Date the funds are expected to arrive in your Stripe account for payouts. This factors in delays
    /// like weekends or bank holidays. May not be specified depending on status of top-up.
    public var expectedAvailabilityDate: Int?
    /// Error code explaining reason for top-up failure if available (see [the errors
    /// section](https://docs.stripe.com/api#errors) for a list of codes).
    public var failureCode: String?
    /// Message to user further explaining reason for top-up failure if available.
    public var failureMessage: String?
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: TopupObject
    /// The source field is deprecated. It might not always be present in the API response.
    @Expandable<Source>
    public var source: String?
    /// Extra information about a top-up. This will appear on your source's bank statement. It must contain
    /// at least one letter.
    public var statementDescriptor: String?
    /// The status of the top-up is either `canceled`, `failed`, `pending`, `reversed`, or `succeeded`.
    public var status: TopupStatus
    /// A string that identifies this top-up as part of a group.
    public var transferGroup: String?

    public enum CodingKeys: String, CodingKey {
        case amount
        case balanceTransaction = "balance_transaction"
        case created
        case currency
        case description
        case expectedAvailabilityDate = "expected_availability_date"
        case failureCode = "failure_code"
        case failureMessage = "failure_message"
        case id
        case livemode
        case metadata
        case object
        case source
        case statementDescriptor = "statement_descriptor"
        case status
        case transferGroup = "transfer_group"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum TopupObject: String, Codable, Sendable {
    case topup
}

/// The status of the top-up is either `canceled`, `failed`, `pending`, `reversed`, or `succeeded`.
public enum TopupStatus: String, Codable, Sendable {
    case canceled
    case failed
    case pending
    case reversed
    case succeeded
}

public struct TopupList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Topup]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

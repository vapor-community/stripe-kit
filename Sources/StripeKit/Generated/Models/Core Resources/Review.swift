//
//  Review.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// Reviews can be used to supplement automated fraud detection with human expertise. Learn more about
/// [Radar](/radar) and reviewing payments [here](https://docs.stripe.com/radar/reviews).
public struct Review: Codable, Sendable {
    /// The ZIP or postal code of the card used, if applicable.
    public var billingZip: String?
    /// The charge associated with this review.
    @Expandable<Charge>
    public var charge: String?
    /// The reason the review was closed, or null if it has not yet been closed. One of `approved`,
    /// `refunded`, `refunded_as_fraud`, `disputed`, `redacted`, `canceled`, `payment_never_settled`, or
    /// `acknowledged`.
    public var closedReason: ReviewClosedReason?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Unique identifier for the object.
    public var id: String
    /// The IP address where the payment originated.
    public var ipAddress: String?
    /// Information related to the location of the payment. Note that this information is an approximation
    /// and attempts to locate the nearest population center - it should not be used to determine a specific
    /// address.
    @Expandable<RadarReviewResourceLocation>
    public var ipAddressLocation: String?
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: ReviewObject
    /// If `true`, the review needs action.
    public var `open`: Bool
    /// The reason the review was opened. One of `rule` or `manual`.
    public var openedReason: ReviewOpenedReason
    /// The PaymentIntent ID associated with this review, if one exists.
    @Expandable<PaymentIntent>
    public var paymentIntent: String?
    /// The reason the review is currently open or closed. One of `rule`, `manual`, `approved`, `refunded`,
    /// `refunded_as_fraud`, `disputed`, `redacted`, `canceled`, `payment_never_settled`, or `acknowledged`.
    public var reason: String
    /// Information related to the browsing session of the user who initiated the payment.
    @Expandable<RadarReviewResourceSession>
    public var session: String?

    public enum CodingKeys: String, CodingKey {
        case billingZip = "billing_zip"
        case charge
        case closedReason = "closed_reason"
        case created
        case id
        case ipAddress = "ip_address"
        case ipAddressLocation = "ip_address_location"
        case livemode
        case object
        case `open`
        case openedReason = "opened_reason"
        case paymentIntent = "payment_intent"
        case reason
        case session
    }
}

/// The reason the review was closed, or null if it has not yet been closed. One of `approved`,
/// `refunded`, `refunded_as_fraud`, `disputed`, `redacted`, `canceled`, `payment_never_settled`, or
/// `acknowledged`.
public enum ReviewClosedReason: String, Codable, Sendable {
    case acknowledged
    case approved
    case canceled
    case disputed
    case paymentNeverSettled = "payment_never_settled"
    case redacted
    case refunded
    case refundedAsFraud = "refunded_as_fraud"
}

/// String representing the object's type. Objects of the same type share the same value.
public enum ReviewObject: String, Codable, Sendable {
    case review
}

/// The reason the review was opened. One of `rule` or `manual`.
public enum ReviewOpenedReason: String, Codable, Sendable {
    case manual
    case rule
}

public struct ReviewList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Review]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

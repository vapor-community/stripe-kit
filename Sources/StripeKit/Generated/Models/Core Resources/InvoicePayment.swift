//
//  InvoicePayment.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// Invoice Payments represent payments made against invoices. Invoice Payments can be accessed in two
/// ways: 1. By expanding the `payments` field on the [Invoice](https://api.stripe.com#invoice)
/// resource. 2. By using the Invoice Payment retrieve and list endpoints. Invoice Payments include the
/// mapping between payment objects, such as Payment Intent, and Invoices. This resource and its
/// endpoints allows you to easily track if a payment is associated with a specific invoice and monitor
/// the allocation details of the payments.
public struct InvoicePayment: Codable, Sendable {
    /// Amount that was actually paid for this invoice, in cents (or local equivalent). This field is null
    /// until the payment is `paid`. This amount can be less than the `amount_requested` if the
    /// PaymentIntent’s `amount_received` is not sufficient to pay all of the invoices that it is attached
    /// to.
    public var amountPaid: Int?
    /// Amount intended to be paid toward this invoice, in cents (or local equivalent)
    public var amountRequested: Int
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// Unique identifier for the object.
    public var id: String
    /// The invoice that was paid.
    @Expandable<Invoice>
    public var invoice: String?
    /// Stripe automatically creates a default InvoicePayment when the invoice is finalized, and keeps it
    /// synchronized with the invoice’s `amount_remaining`. The PaymentIntent associated with the default
    /// payment can’t be edited or canceled directly.
    public var isDefault: Bool
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: InvoicePaymentObject
    @Expandable<InvoicesPaymentsInvoicePaymentAssociatedPayment>
    public var payment: String?
    /// The status of the payment, one of `open`, `paid`, or `canceled`.
    public var status: String
    @Expandable<InvoicesPaymentsInvoicePaymentStatusTransitions>
    public var statusTransitions: String?

    public enum CodingKeys: String, CodingKey {
        case amountPaid = "amount_paid"
        case amountRequested = "amount_requested"
        case created
        case currency
        case id
        case invoice
        case isDefault = "is_default"
        case livemode
        case object
        case payment
        case status
        case statusTransitions = "status_transitions"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum InvoicePaymentObject: String, Codable, Sendable {
    case invoicePayment = "invoice_payment"
}

public struct InvoicePaymentList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [InvoicePayment]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

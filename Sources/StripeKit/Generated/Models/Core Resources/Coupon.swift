//
//  Coupon.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// A coupon contains information about a percent-off or amount-off discount you might want to apply to
/// a customer. Coupons may be applied to [subscriptions](https://api.stripe.com#subscriptions),
/// [invoices](https://api.stripe.com#invoices), [checkout
/// sessions](https://docs.stripe.com/api/checkout/sessions), [quotes](https://api.stripe.com#quotes),
/// and more. Coupons do not work with conventional one-off
/// [charges](https://api.stripe.com#create_charge) or [payment
/// intents](https://docs.stripe.com/api/payment_intents).
public struct Coupon: Codable, Sendable {
    /// Amount (in the `currency` specified) that will be taken off the subtotal of any invoices for this
    /// customer.
    public var amountOff: Int?
    @Expandable<CouponAppliesTo>
    public var appliesTo: String?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// If `amount_off` has been set, the three-letter [ISO code for the
    /// currency](https://stripe.com/docs/currencies) of the amount to take off.
    public var currency: String?
    /// Coupons defined in each available currency option. Each key must be a three-letter [ISO currency
    /// code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported
    /// currency](https://stripe.com/docs/currencies).
    public var currencyOptions: [String: CouponCurrencyOption]?
    /// One of `forever`, `once`, or `repeating`. Describes how long a customer who applies this coupon will
    /// get the discount.
    public var duration: CouponDuration
    /// If `duration` is `repeating`, the number of months the coupon applies. Null if coupon `duration` is
    /// `forever` or `once`.
    public var durationInMonths: Int?
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Maximum number of times this coupon can be redeemed, in total, across all customers, before it is no
    /// longer valid.
    public var maxRedemptions: Int?
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]?
    /// Name of the coupon displayed to customers on for instance invoices or receipts.
    public var name: String?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: CouponObject
    /// Percent that will be taken off the subtotal of any invoices for this customer for the duration of
    /// the coupon. For example, a coupon with percent_off of 50 will make a $ (or local equivalent)100
    /// invoice $ (or local equivalent)50 instead.
    public var percentOff: Double?
    /// Date after which the coupon can no longer be redeemed.
    public var redeemBy: Int?
    /// Number of times this coupon has been applied to a customer.
    public var timesRedeemed: Int
    /// Taking account of the above properties, whether this coupon can still be applied to a customer.
    public var valid: Bool

    public enum CodingKeys: String, CodingKey {
        case amountOff = "amount_off"
        case appliesTo = "applies_to"
        case created
        case currency
        case currencyOptions = "currency_options"
        case duration
        case durationInMonths = "duration_in_months"
        case id
        case livemode
        case maxRedemptions = "max_redemptions"
        case metadata
        case name
        case object
        case percentOff = "percent_off"
        case redeemBy = "redeem_by"
        case timesRedeemed = "times_redeemed"
        case valid
    }
}

/// One of `forever`, `once`, or `repeating`. Describes how long a customer who applies this coupon will
/// get the discount.
public enum CouponDuration: String, Codable, Sendable {
    case forever
    case once
    case repeating
}

/// String representing the object's type. Objects of the same type share the same value.
public enum CouponObject: String, Codable, Sendable {
    case coupon
}

public struct CouponList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Coupon]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

//
//  Customer.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// This object represents a customer of your business. Use it to [create recurring
/// charges](https://docs.stripe.com/invoicing/customer), [save
/// payment](https://docs.stripe.com/payments/save-during-payment) and contact information, and track
/// payments that belong to the same customer.
public struct Customer: Codable, Sendable {
    /// The customer's address.
    @Expandable<Address>
    public var address: String?
    /// The current balance, if any, that's stored on the customer in their default currency. If negative,
    /// the customer has credit to apply to their next invoice. If positive, the customer has an amount owed
    /// that's added to their next invoice. The balance only considers amounts that Stripe hasn't
    /// successfully applied to any invoice. It doesn't reflect unpaid invoices. This balance is only taken
    /// into account after invoices finalize. For multi-currency balances, see
    /// [invoice_credit_balance](https://docs.stripe.com/api/customers/object#customer_object-invoice_credit_balance).
    public var balance: Int?
    /// The customer's business name.
    public var businessName: String?
    /// The current funds being held by Stripe on behalf of the customer. You can apply these funds towards
    /// payment intents when the source is "cash_balance". The `settings[reconciliation_mode]` field
    /// describes if these funds apply to these payment intents manually or automatically.
    @Expandable<CashBalance>
    public var cashBalance: String?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO code for the currency](https://stripe.com/docs/currencies) the customer can be
    /// charged in for recurring billing purposes.
    public var currency: String?
    /// The ID of an Account representing a customer. You can use this ID with any v1 API that accepts a
    /// customer_account parameter.
    public var customerAccount: String?
    /// ID of the default payment source for the customer. If you use payment methods created through the
    /// PaymentMethods API, see the
    /// [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method)
    /// field instead.
    @Expandable<PaymentSource>
    public var defaultSource: String?
    /// Tracks the most recent state change on any invoice belonging to the customer. Paying an invoice or
    /// marking it uncollectible via the API will set this field to false. An automatic payment failure or
    /// passing the `invoice.due_date` will set this field to `true`. If an invoice becomes uncollectible by
    /// [dunning](https://docs.stripe.com/billing/automatic-collection), `delinquent` doesn't reset to
    /// `false`. If you care whether the customer has paid their most recent subscription invoice, use
    /// `subscription.status` instead. Paying or marking uncollectible any customer invoice regardless of
    /// whether it is the latest invoice for a subscription will always set this field to `false`.
    public var delinquent: Bool?
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String?
    /// Describes the current discount active on the customer, if there is one.
    @Expandable<Discount>
    public var discount: String?
    /// The customer's email address.
    public var email: String?
    /// Unique identifier for the object.
    public var id: String
    /// The customer's individual name.
    public var individualName: String?
    /// The current multi-currency balances, if any, that's stored on the customer. If positive in a
    /// currency, the customer has a credit to apply to their next invoice denominated in that currency. If
    /// negative, the customer has an amount owed that's added to their next invoice denominated in that
    /// currency. These balances don't apply to unpaid invoices. They solely track amounts that Stripe
    /// hasn't successfully applied to any invoice. Stripe only applies a balance in a specific currency to
    /// an invoice after that invoice (which is in the same currency) finalizes.
    public var invoiceCreditBalance: [String: Int]?
    /// The prefix for the customer used to generate unique invoice numbers.
    public var invoicePrefix: String?
    @Expandable<InvoiceSettingCustomerSetting>
    public var invoiceSettings: String?
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]?
    /// The customer's full name or business name.
    public var name: String?
    /// The suffix of the customer's next invoice number (for example, 0001). When the account uses account
    /// level sequencing, this parameter is ignored in API requests and the field omitted in API responses.
    public var nextInvoiceSequence: Int?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: CustomerObject
    /// The customer's phone number.
    public var phone: String?
    /// The customer's preferred locales (languages), ordered by preference.
    public var preferredLocales: [String]?
    /// Mailing and shipping address for the customer. Appears on invoices emailed to this customer.
    @Expandable<Shipping>
    public var shipping: String?
    /// The customer's payment sources, if any.
    public var sources: PaymentSourceList?
    /// The customer's current subscriptions, if any.
    public var subscriptions: SubscriptionList?
    @Expandable<CustomerTax>
    public var tax: String?
    /// Describes the customer's tax exemption status, which is `none`, `exempt`, or `reverse`. When set to
    /// `reverse`, invoice and receipt PDFs include the following text: **"Reverse charge"**.
    public var taxExempt: CustomerTaxExempt?
    /// The customer's tax IDs.
    public var taxIds: TaxIdList?
    /// ID of the test clock that this customer belongs to.
    @Expandable<TestClock>
    public var testClock: String?

    public enum CodingKeys: String, CodingKey {
        case address
        case balance
        case businessName = "business_name"
        case cashBalance = "cash_balance"
        case created
        case currency
        case customerAccount = "customer_account"
        case defaultSource = "default_source"
        case delinquent
        case description
        case discount
        case email
        case id
        case individualName = "individual_name"
        case invoiceCreditBalance = "invoice_credit_balance"
        case invoicePrefix = "invoice_prefix"
        case invoiceSettings = "invoice_settings"
        case livemode
        case metadata
        case name
        case nextInvoiceSequence = "next_invoice_sequence"
        case object
        case phone
        case preferredLocales = "preferred_locales"
        case shipping
        case sources
        case subscriptions
        case tax
        case taxExempt = "tax_exempt"
        case taxIds = "tax_ids"
        case testClock = "test_clock"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum CustomerObject: String, Codable, Sendable {
    case customer
}

/// Describes the customer's tax exemption status, which is `none`, `exempt`, or `reverse`. When set to
/// `reverse`, invoice and receipt PDFs include the following text: **"Reverse charge"**.
public enum CustomerTaxExempt: String, Codable, Sendable {
    case exempt
    case none
    case reverse
}

public struct CustomerList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Customer]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

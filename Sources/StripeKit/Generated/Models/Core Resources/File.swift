//
//  File.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// This object represents files hosted on Stripe's servers. You can upload files with the [create
/// file](https://api.stripe.com#create_file) request (for example, when uploading dispute evidence).
/// Stripe also creates files independently (for example, the results of a [Sigma scheduled
/// query](#scheduled_queries)). Related guide: [File upload guide](https://docs.stripe.com/file-upload)
public struct File: Codable, Sendable {
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// The file expires and isn't available at this time in epoch seconds.
    public var expiresAt: Int?
    /// The suitable name for saving the file to a filesystem.
    public var filename: String?
    /// Unique identifier for the object.
    public var id: String
    /// A list of [file links](https://api.stripe.com#file_links) that point at this file.
    public var links: FileLinkList?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: FileObject
    /// The [purpose](https://docs.stripe.com/file-upload#uploading-a-file) of the uploaded file.
    public var purpose: FilePurpose
    /// The size of the file object in bytes.
    public var size: Int
    /// A suitable title for the document.
    public var title: String?
    /// The returned file type (for example, `csv`, `pdf`, `jpg`, or `png`).
    public var type: String?
    /// Use your live secret API key to download the file from this URL.
    public var url: String?

    public enum CodingKeys: String, CodingKey {
        case created
        case expiresAt = "expires_at"
        case filename
        case id
        case links
        case object
        case purpose
        case size
        case title
        case type
        case url
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum FileObject: String, Codable, Sendable {
    case file
}

/// The [purpose](https://docs.stripe.com/file-upload#uploading-a-file) of the uploaded file.
public enum FilePurpose: String, Codable, Sendable {
    case accountRequirement = "account_requirement"
    case additionalVerification = "additional_verification"
    case businessIcon = "business_icon"
    case businessLogo = "business_logo"
    case customerSignature = "customer_signature"
    case disputeEvidence = "dispute_evidence"
    case documentProviderIdentityDocument = "document_provider_identity_document"
    case financeReportRun = "finance_report_run"
    case financialAccountStatement = "financial_account_statement"
    case identityDocument = "identity_document"
    case identityDocumentDownloadable = "identity_document_downloadable"
    case issuingRegulatoryReporting = "issuing_regulatory_reporting"
    case pciDocument = "pci_document"
    case platformTermsOfService = "platform_terms_of_service"
    case selfie
    case sigmaScheduledQuery = "sigma_scheduled_query"
    case taxDocumentUserUpload = "tax_document_user_upload"
    case terminalAndroidApk = "terminal_android_apk"
    case terminalReaderSplashscreen = "terminal_reader_splashscreen"
}

public struct FileList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [File]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

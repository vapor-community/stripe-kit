//
//  Plan.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// You can now model subscriptions more flexibly using the [Prices API](https://api.stripe.com#prices).
/// It replaces the Plans API and is backwards compatible to simplify your migration. Plans define the
/// base price, currency, and billing cycle for recurring purchases of products.
/// [Products](https://api.stripe.com#products) help you track inventory or provisioning, and plans help
/// you track pricing. Different physical goods or levels of service should be represented by products,
/// and pricing options should be represented by plans. This approach lets you change prices without
/// having to change your provisioning scheme. For example, you might have a single "gold" product that
/// has plans for $10/month, $100/year, €9/month, and €90/year. Related guides: [Set up a
/// subscription](https://docs.stripe.com/billing/subscriptions/set-up-subscription) and more about
/// [products and prices](https://docs.stripe.com/products-prices/overview).
public struct Plan: Codable, Sendable {
    /// Whether the plan can be used for new purchases.
    public var active: Bool
    /// The unit amount in cents (or local equivalent) to be charged, represented as a whole integer if
    /// possible. Only set if `billing_scheme=per_unit`.
    public var amount: Int?
    /// The unit amount in cents (or local equivalent) to be charged, represented as a decimal string with
    /// at most 12 decimal places. Only set if `billing_scheme=per_unit`.
    public var amountDecimal: String?
    /// Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates
    /// that the fixed amount (specified in `amount`) will be charged per unit in `quantity` (for plans with
    /// `usage_type=licensed`), or per unit of total usage (for plans with `usage_type=metered`). `tiered`
    /// indicates that the unit pricing will be computed using a tiering strategy as defined using the
    /// `tiers` and `tiers_mode` attributes.
    public var billingScheme: PlanBillingScheme
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// Unique identifier for the object.
    public var id: String
    /// The frequency at which a subscription is billed. One of `day`, `week`, `month` or `year`.
    public var interval: PlanInterval
    /// The number of intervals (specified in the `interval` attribute) between subscription billings. For
    /// example, `interval=month` and `interval_count=3` bills every 3 months.
    public var intervalCount: Int
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]?
    /// The meter tracking the usage of a metered price
    public var meter: String?
    /// A brief description of the plan, hidden from customers.
    public var nickname: String?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: PlanObject
    /// The product whose pricing this plan determines.
    @Expandable<Product>
    public var product: String?
    /// Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to
    /// `tiered`. See also the documentation for `billing_scheme`.
    @ExpandableCollection<PlanTier>
    public var tiers: [String]?
    /// Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the
    /// maximum quantity within a period determines the per unit price. In `graduated` tiering, pricing can
    /// change as the quantity grows.
    public var tiersMode: PlanTiersMode?
    /// Apply a transformation to the reported usage or set quantity before computing the amount billed.
    /// Cannot be combined with `tiers`.
    @Expandable<TransformUsage>
    public var transformUsage: String?
    /// Default number of trial days when subscribing a customer to this plan using
    /// [`trial_from_plan=true`](https://docs.stripe.com/api#create_subscription-trial_from_plan).
    public var trialPeriodDays: Int?
    /// Configures how the quantity per period should be determined. Can be either `metered` or `licensed`.
    /// `licensed` automatically bills the `quantity` set when adding it to a subscription. `metered`
    /// aggregates the total usage based on usage records. Defaults to `licensed`.
    public var usageType: PlanUsageType

    public enum CodingKeys: String, CodingKey {
        case active
        case amount
        case amountDecimal = "amount_decimal"
        case billingScheme = "billing_scheme"
        case created
        case currency
        case id
        case interval
        case intervalCount = "interval_count"
        case livemode
        case metadata
        case meter
        case nickname
        case object
        case product
        case tiers
        case tiersMode = "tiers_mode"
        case transformUsage = "transform_usage"
        case trialPeriodDays = "trial_period_days"
        case usageType = "usage_type"
    }
}

/// Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates
/// that the fixed amount (specified in `amount`) will be charged per unit in `quantity` (for plans with
/// `usage_type=licensed`), or per unit of total usage (for plans with `usage_type=metered`). `tiered`
/// indicates that the unit pricing will be computed using a tiering strategy as defined using the
/// `tiers` and `tiers_mode` attributes.
public enum PlanBillingScheme: String, Codable, Sendable {
    case perUnit = "per_unit"
    case tiered
}

/// The frequency at which a subscription is billed. One of `day`, `week`, `month` or `year`.
public enum PlanInterval: String, Codable, Sendable {
    case day
    case month
    case week
    case year
}

/// String representing the object's type. Objects of the same type share the same value.
public enum PlanObject: String, Codable, Sendable {
    case plan
}

/// Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the
/// maximum quantity within a period determines the per unit price. In `graduated` tiering, pricing can
/// change as the quantity grows.
public enum PlanTiersMode: String, Codable, Sendable {
    case graduated
    case volume
}

/// Configures how the quantity per period should be determined. Can be either `metered` or `licensed`.
/// `licensed` automatically bills the `quantity` set when adding it to a subscription. `metered`
/// aggregates the total usage based on usage records. Defaults to `licensed`.
public enum PlanUsageType: String, Codable, Sendable {
    case licensed
    case metered
}

public struct PlanList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Plan]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

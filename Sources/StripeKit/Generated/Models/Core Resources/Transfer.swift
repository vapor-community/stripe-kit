//
//  Transfer.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// A `Transfer` object is created when you move funds between Stripe accounts as part of Connect.
/// Before April 6, 2017, transfers also represented movement of funds from a Stripe account to a card
/// or bank account. This behavior has since been split out into a
/// [Payout](https://api.stripe.com#payout_object) object, with corresponding payout endpoints. For more
/// information, read about the [transfer/payout split](https://docs.stripe.com/transfer-payout-split).
/// Related guide: [Creating separate charges and
/// transfers](https://docs.stripe.com/connect/separate-charges-and-transfers)
public struct Transfer: Codable, Sendable {
    /// Amount in cents (or local equivalent) to be transferred.
    public var amount: Int
    /// Amount in cents (or local equivalent) reversed (can be less than the amount attribute on the
    /// transfer if a partial reversal was issued).
    public var amountReversed: Int
    /// Balance transaction that describes the impact of this transfer on your account balance.
    @Expandable<BalanceTransaction>
    public var balanceTransaction: String?
    /// Time that this record of the transfer was first created.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String?
    /// ID of the Stripe account the transfer was sent to.
    @Expandable<Account>
    public var destination: String?
    /// If the destination is a Stripe account, this will be the ID of the payment that the destination
    /// account received for the transfer.
    @Expandable<Charge>
    public var destinationPayment: String?
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: TransferObject
    /// A list of reversals that have been applied to the transfer.
    public var reversals: TransferReversalList
    /// Whether the transfer has been fully reversed. If the transfer is only partially reversed, this
    /// attribute will still be false.
    public var reversed: Bool
    /// ID of the charge that was used to fund the transfer. If null, the transfer was funded from the
    /// available balance.
    @Expandable<Charge>
    public var sourceTransaction: String?
    /// The source balance this transfer came from. One of `card`, `fpx`, or `bank_account`.
    public var sourceType: String?
    /// A string that identifies this transaction as part of a group. See the [Connect
    /// documentation](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-options) for
    /// details.
    public var transferGroup: String?

    public enum CodingKeys: String, CodingKey {
        case amount
        case amountReversed = "amount_reversed"
        case balanceTransaction = "balance_transaction"
        case created
        case currency
        case description
        case destination
        case destinationPayment = "destination_payment"
        case id
        case livemode
        case metadata
        case object
        case reversals
        case reversed
        case sourceTransaction = "source_transaction"
        case sourceType = "source_type"
        case transferGroup = "transfer_group"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum TransferObject: String, Codable, Sendable {
    case transfer
}

public struct TransferList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Transfer]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

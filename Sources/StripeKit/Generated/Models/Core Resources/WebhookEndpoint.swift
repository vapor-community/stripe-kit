//
//  WebhookEndpoint.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// You can configure [webhook endpoints](https://docs.stripe.com/webhooks/) via the API to be notified
/// about events that happen in your Stripe account or connected accounts. Most users configure webhooks
/// from [the dashboard](https://dashboard.stripe.com/webhooks), which provides a user interface for
/// registering and testing your webhook endpoints. Related guide: [Setting up
/// webhooks](https://docs.stripe.com/webhooks/configure)
public struct WebhookEndpoint: Codable, Sendable {
    /// The API version events are rendered as for this webhook endpoint.
    public var apiVersion: String?
    /// The ID of the associated Connect application.
    public var application: String?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// An optional description of what the webhook is used for.
    public var description: String?
    /// The list of events to enable for this endpoint. `['*']` indicates that all events are enabled,
    /// except those that require explicit selection.
    public var enabledEvents: [String]
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: WebhookEndpointObject
    /// The endpoint's secret, used to generate [webhook
    /// signatures](https://docs.stripe.com/webhooks/signatures). Only returned at creation.
    public var secret: String?
    /// The status of the webhook. It can be `enabled` or `disabled`.
    public var status: String
    /// The URL of the webhook endpoint.
    public var url: String

    public enum CodingKeys: String, CodingKey {
        case apiVersion = "api_version"
        case application
        case created
        case description
        case enabledEvents = "enabled_events"
        case id
        case livemode
        case metadata
        case object
        case secret
        case status
        case url
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum WebhookEndpointObject: String, Codable, Sendable {
    case webhookEndpoint = "webhook_endpoint"
}

public struct WebhookEndpointList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [WebhookEndpoint]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

//
//  Subscription.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// Subscriptions allow you to charge a customer on a recurring basis. Related guide: [Creating
/// subscriptions](https://docs.stripe.com/billing/subscriptions/creating)
public struct Subscription: Codable, Sendable {
    /// ID of the Connect Application that created the subscription.
    @Expandable<Application>
    public var application: String?
    /// A non-negative decimal between 0 and 100, with at most two decimal places. This represents the
    /// percentage of the subscription invoice total that will be transferred to the application owner's
    /// Stripe account.
    public var applicationFeePercent: Double?
    @Expandable<SubscriptionAutomaticTax>
    public var automaticTax: String?
    /// The reference point that aligns future [billing
    /// cycle](https://docs.stripe.com/subscriptions/billing-cycle) dates. It sets the day of week for
    /// `week` intervals, the day of month for `month` and `year` intervals, and the month of year for
    /// `year` intervals. The timestamp is in UTC format.
    public var billingCycleAnchor: Int
    /// The fixed values used to calculate the `billing_cycle_anchor`.
    @Expandable<SubscriptionsResourceBillingCycleAnchorConfig>
    public var billingCycleAnchorConfig: String?
    @Expandable<SubscriptionsResourceBillingMode>
    public var billingMode: String?
    /// Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing
    /// period
    @Expandable<SubscriptionBillingThresholds>
    public var billingThresholds: String?
    /// A date in the future at which the subscription will automatically get canceled
    public var cancelAt: Int?
    /// Whether this subscription will (if `status=active`) or did (if `status=canceled`) cancel at the end
    /// of the current billing period.
    public var cancelAtPeriodEnd: Bool
    /// If the subscription has been canceled, the date of that cancellation. If the subscription was
    /// canceled with `cancel_at_period_end`, `canceled_at` will reflect the time of the most recent update
    /// request, not the end of the subscription period when the subscription is automatically moved to a
    /// canceled state.
    public var canceledAt: Int?
    /// Details about why this subscription was cancelled
    @Expandable<CancellationDetails>
    public var cancellationDetails: String?
    /// Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt
    /// to pay this subscription at the end of the cycle using the default source attached to the customer.
    /// When sending an invoice, Stripe will email your customer an invoice with payment instructions and
    /// mark the subscription as `active`.
    public var collectionMethod: SubscriptionCollectionMethod
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// ID of the customer who owns the subscription.
    @Expandable<Customer>
    public var customer: String?
    /// ID of the account representing the customer who owns the subscription.
    public var customerAccount: String?
    /// Number of days a customer has to pay invoices generated by this subscription. This value will be
    /// `null` for subscriptions where `collection_method=charge_automatically`.
    public var daysUntilDue: Int?
    /// ID of the default payment method for the subscription. It must belong to the customer associated
    /// with the subscription. This takes precedence over `default_source`. If neither are set, invoices
    /// will use the customer's
    /// [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method)
    /// or [default_source](https://docs.stripe.com/api/customers/object#customer_object-default_source).
    @Expandable<PaymentMethod>
    public var defaultPaymentMethod: String?
    /// ID of the default payment source for the subscription. It must belong to the customer associated
    /// with the subscription and be in a chargeable state. If `default_payment_method` is also set,
    /// `default_payment_method` will take precedence. If neither are set, invoices will use the customer's
    /// [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method)
    /// or [default_source](https://docs.stripe.com/api/customers/object#customer_object-default_source).
    @Expandable<PaymentSource>
    public var defaultSource: String?
    /// The tax rates that will apply to any subscription item that does not have `tax_rates` set. Invoices
    /// created will have their `default_tax_rates` populated from the subscription.
    @ExpandableCollection<TaxRate>
    public var defaultTaxRates: [String]?
    /// The subscription's description, meant to be displayable to the customer. Use this field to
    /// optionally store an explanation of the subscription for rendering in Stripe surfaces and certain
    /// local payment methods UIs.
    public var description: String?
    /// The discounts applied to the subscription. Subscription item discounts are applied before
    /// subscription discounts. Use `expand[]=discounts` to expand each discount.
    @ExpandableCollection<Discount>
    public var discounts: [String]?
    /// If the subscription has ended, the date the subscription ended.
    public var endedAt: Int?
    /// Unique identifier for the object.
    public var id: String
    @Expandable<SubscriptionsResourceSubscriptionInvoiceSettings>
    public var invoiceSettings: String?
    /// List of subscription items, each with an attached price.
    public var items: SubscriptionItemList
    /// The most recent invoice this subscription has generated over its lifecycle (for example, when it
    /// cycles or is updated).
    @Expandable<Invoice>
    public var latestInvoice: String?
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]
    /// Specifies the approximate timestamp on which any pending invoice items will be billed according to
    /// the schedule provided at `pending_invoice_item_interval`.
    public var nextPendingInvoiceItemInvoice: Int?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: SubscriptionObject
    /// The account (if any) the charge was made on behalf of for charges associated with this subscription.
    /// See the [Connect documentation](https://docs.stripe.com/connect/subscriptions#on-behalf-of) for
    /// details.
    @Expandable<Account>
    public var onBehalfOf: String?
    /// If specified, payment collection for this subscription will be paused. Note that the subscription
    /// status will be unchanged and will not be updated to `paused`. Learn more about [pausing
    /// collection](https://docs.stripe.com/billing/subscriptions/pause-payment).
    @Expandable<SubscriptionsResourcePauseCollection>
    public var pauseCollection: String?
    /// Payment settings passed on to invoices created by the subscription.
    @Expandable<SubscriptionsResourcePaymentSettings>
    public var paymentSettings: String?
    /// Specifies an interval for how often to bill for any pending invoice items. It is analogous to
    /// calling [Create an invoice](https://docs.stripe.com/api#create_invoice) for the given subscription
    /// at the specified interval.
    @Expandable<SubscriptionPendingInvoiceItemInterval>
    public var pendingInvoiceItemInterval: String?
    /// You can use this [SetupIntent](https://docs.stripe.com/api/setup_intents) to collect user
    /// authentication when creating a subscription without immediate payment or updating a subscription's
    /// payment method, allowing you to optimize for off-session payments. Learn more in the [SCA Migration
    /// Guide](https://docs.stripe.com/billing/migration/strong-customer-authentication#scenario-2).
    @Expandable<SetupIntent>
    public var pendingSetupIntent: String?
    /// If specified, [pending updates](https://docs.stripe.com/billing/subscriptions/pending-updates) that
    /// will be applied to the subscription once the `latest_invoice` has been paid.
    @Expandable<SubscriptionsResourcePendingUpdate>
    public var pendingUpdate: String?
    /// The schedule attached to the subscription
    @Expandable<SubscriptionSchedule>
    public var schedule: String?
    /// Date when the subscription was first created. The date might differ from the `created` date due to
    /// backdating.
    public var startDate: Int
    /// Possible values are `incomplete`, `incomplete_expired`, `trialing`, `active`, `past_due`,
    /// `canceled`, `unpaid`, or `paused`. For `collection_method=charge_automatically` a subscription moves
    /// into `incomplete` if the initial payment attempt fails. A subscription in this status can only have
    /// metadata and default_source updated. Once the first invoice is paid, the subscription moves into an
    /// `active` status. If the first invoice is not paid within 23 hours, the subscription transitions to
    /// `incomplete_expired`. This is a terminal status, the open invoice will be voided and no further
    /// invoices will be generated. A subscription that is currently in a trial period is `trialing` and
    /// moves to `active` when the trial period is over. A subscription can only enter a `paused` status
    /// [when a trial ends without a payment
    /// method](https://docs.stripe.com/billing/subscriptions/trials#create-free-trials-without-payment). A
    /// `paused` subscription doesn't generate invoices and can be resumed after your customer adds their
    /// payment method. The `paused` status is different from [pausing
    /// collection](https://docs.stripe.com/billing/subscriptions/pause-payment), which still generates
    /// invoices and leaves the subscription's status unchanged. If subscription
    /// `collection_method=charge_automatically`, it becomes `past_due` when payment is required but cannot
    /// be paid (due to failed payment or awaiting additional user actions). Once Stripe has exhausted all
    /// payment retry attempts, the subscription will become `canceled` or `unpaid` (depending on your
    /// subscriptions settings). If subscription `collection_method=send_invoice` it becomes `past_due` when
    /// its invoice is not paid by the due date, and `canceled` or `unpaid` if it is still not paid by an
    /// additional deadline after that. Note that when a subscription has a status of `unpaid`, no
    /// subsequent invoices will be attempted (invoices will be created, but then immediately automatically
    /// closed). After receiving updated payment information from a customer, you may choose to reopen and
    /// pay their closed invoices.
    public var status: SubscriptionStatus
    /// ID of the test clock this subscription belongs to.
    @Expandable<TestClock>
    public var testClock: String?
    /// The account (if any) the subscription's payments will be attributed to for tax reporting, and where
    /// funds from each payment will be transferred to for each of the subscription's invoices.
    @Expandable<SubscriptionTransferData>
    public var transferData: String?
    /// If the subscription has a trial, the end of that trial.
    public var trialEnd: Int?
    /// Settings related to subscription trials.
    @Expandable<SubscriptionsTrialsResourceTrialSettings>
    public var trialSettings: String?
    /// If the subscription has a trial, the beginning of that trial.
    public var trialStart: Int?

    public enum CodingKeys: String, CodingKey {
        case application
        case applicationFeePercent = "application_fee_percent"
        case automaticTax = "automatic_tax"
        case billingCycleAnchor = "billing_cycle_anchor"
        case billingCycleAnchorConfig = "billing_cycle_anchor_config"
        case billingMode = "billing_mode"
        case billingThresholds = "billing_thresholds"
        case cancelAt = "cancel_at"
        case cancelAtPeriodEnd = "cancel_at_period_end"
        case canceledAt = "canceled_at"
        case cancellationDetails = "cancellation_details"
        case collectionMethod = "collection_method"
        case created
        case currency
        case customer
        case customerAccount = "customer_account"
        case daysUntilDue = "days_until_due"
        case defaultPaymentMethod = "default_payment_method"
        case defaultSource = "default_source"
        case defaultTaxRates = "default_tax_rates"
        case description
        case discounts
        case endedAt = "ended_at"
        case id
        case invoiceSettings = "invoice_settings"
        case items
        case latestInvoice = "latest_invoice"
        case livemode
        case metadata
        case nextPendingInvoiceItemInvoice = "next_pending_invoice_item_invoice"
        case object
        case onBehalfOf = "on_behalf_of"
        case pauseCollection = "pause_collection"
        case paymentSettings = "payment_settings"
        case pendingInvoiceItemInterval = "pending_invoice_item_interval"
        case pendingSetupIntent = "pending_setup_intent"
        case pendingUpdate = "pending_update"
        case schedule
        case startDate = "start_date"
        case status
        case testClock = "test_clock"
        case transferData = "transfer_data"
        case trialEnd = "trial_end"
        case trialSettings = "trial_settings"
        case trialStart = "trial_start"
    }
}

/// Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt
/// to pay this subscription at the end of the cycle using the default source attached to the customer.
/// When sending an invoice, Stripe will email your customer an invoice with payment instructions and
/// mark the subscription as `active`.
public enum SubscriptionCollectionMethod: String, Codable, Sendable {
    case chargeAutomatically = "charge_automatically"
    case sendInvoice = "send_invoice"
}

/// String representing the object's type. Objects of the same type share the same value.
public enum SubscriptionObject: String, Codable, Sendable {
    case subscription
}

/// Possible values are `incomplete`, `incomplete_expired`, `trialing`, `active`, `past_due`,
/// `canceled`, `unpaid`, or `paused`. For `collection_method=charge_automatically` a subscription moves
/// into `incomplete` if the initial payment attempt fails. A subscription in this status can only have
/// metadata and default_source updated. Once the first invoice is paid, the subscription moves into an
/// `active` status. If the first invoice is not paid within 23 hours, the subscription transitions to
/// `incomplete_expired`. This is a terminal status, the open invoice will be voided and no further
/// invoices will be generated. A subscription that is currently in a trial period is `trialing` and
/// moves to `active` when the trial period is over. A subscription can only enter a `paused` status
/// [when a trial ends without a payment
/// method](https://docs.stripe.com/billing/subscriptions/trials#create-free-trials-without-payment). A
/// `paused` subscription doesn't generate invoices and can be resumed after your customer adds their
/// payment method. The `paused` status is different from [pausing
/// collection](https://docs.stripe.com/billing/subscriptions/pause-payment), which still generates
/// invoices and leaves the subscription's status unchanged. If subscription
/// `collection_method=charge_automatically`, it becomes `past_due` when payment is required but cannot
/// be paid (due to failed payment or awaiting additional user actions). Once Stripe has exhausted all
/// payment retry attempts, the subscription will become `canceled` or `unpaid` (depending on your
/// subscriptions settings). If subscription `collection_method=send_invoice` it becomes `past_due` when
/// its invoice is not paid by the due date, and `canceled` or `unpaid` if it is still not paid by an
/// additional deadline after that. Note that when a subscription has a status of `unpaid`, no
/// subsequent invoices will be attempted (invoices will be created, but then immediately automatically
/// closed). After receiving updated payment information from a customer, you may choose to reopen and
/// pay their closed invoices.
public enum SubscriptionStatus: String, Codable, Sendable {
    case active
    case canceled
    case incomplete
    case incompleteExpired = "incomplete_expired"
    case pastDue = "past_due"
    case paused
    case trialing
    case unpaid
}

public struct SubscriptionList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Subscription]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

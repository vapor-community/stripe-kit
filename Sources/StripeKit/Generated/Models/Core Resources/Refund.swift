//
//  Refund.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// Refund objects allow you to refund a previously created charge that isn't refunded yet. Funds are
/// refunded to the credit or debit card that's initially charged. Related guide:
/// [Refunds](https://docs.stripe.com/refunds)
public struct Refund: Codable, Sendable {
    /// Amount, in cents (or local equivalent).
    public var amount: Int
    /// Balance transaction that describes the impact on your account balance.
    @Expandable<BalanceTransaction>
    public var balanceTransaction: String?
    /// ID of the charge that's refunded.
    @Expandable<Charge>
    public var charge: String?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// An arbitrary string attached to the object. You can use this for displaying to users (available on
    /// non-card refunds only).
    public var description: String?
    @Expandable<RefundDestinationDetails>
    public var destinationDetails: String?
    /// After the refund fails, this balance transaction describes the adjustment made on your account
    /// balance that reverses the initial balance transaction.
    @Expandable<BalanceTransaction>
    public var failureBalanceTransaction: String?
    /// Provides the reason for the refund failure. Possible values are: `lost_or_stolen_card`,
    /// `expired_or_canceled_card`, `charge_for_pending_refund_disputed`, `insufficient_funds`, `declined`,
    /// `merchant_request`, or `unknown`.
    public var failureReason: String?
    /// Unique identifier for the object.
    public var id: String
    /// For payment methods without native refund support (for example, Konbini, PromptPay), provide an
    /// email address for the customer to receive refund instructions.
    public var instructionsEmail: String?
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]?
    @Expandable<RefundNextAction>
    public var nextAction: String?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: RefundObject
    /// ID of the PaymentIntent that's refunded.
    @Expandable<PaymentIntent>
    public var paymentIntent: String?
    /// Provides the reason for why the refund is pending. Possible values are: `processing`,
    /// `insufficient_funds`, or `charge_pending`.
    public var pendingReason: RefundPendingReason?
    @Expandable<PaymentFlowsPaymentIntentPresentmentDetails>
    public var presentmentDetails: String?
    /// Reason for the refund, which is either user-provided (`duplicate`, `fraudulent`, or
    /// `requested_by_customer`) or generated by Stripe internally (`expired_uncaptured_charge`).
    public var reason: RefundReason?
    /// This is the transaction number that appears on email receipts sent for this refund.
    public var receiptNumber: String?
    /// The transfer reversal that's associated with the refund. Only present if the charge came from
    /// another Stripe account.
    @Expandable<TransferReversal>
    public var sourceTransferReversal: String?
    /// Status of the refund. This can be `pending`, `requires_action`, `succeeded`, `failed`, or
    /// `canceled`. Learn more about [failed refunds](https://docs.stripe.com/refunds#failed-refunds).
    public var status: String?
    /// This refers to the transfer reversal object if the accompanying transfer reverses. This is only
    /// applicable if the charge was created using the destination parameter.
    @Expandable<TransferReversal>
    public var transferReversal: String?

    public enum CodingKeys: String, CodingKey {
        case amount
        case balanceTransaction = "balance_transaction"
        case charge
        case created
        case currency
        case description
        case destinationDetails = "destination_details"
        case failureBalanceTransaction = "failure_balance_transaction"
        case failureReason = "failure_reason"
        case id
        case instructionsEmail = "instructions_email"
        case metadata
        case nextAction = "next_action"
        case object
        case paymentIntent = "payment_intent"
        case pendingReason = "pending_reason"
        case presentmentDetails = "presentment_details"
        case reason
        case receiptNumber = "receipt_number"
        case sourceTransferReversal = "source_transfer_reversal"
        case status
        case transferReversal = "transfer_reversal"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum RefundObject: String, Codable, Sendable {
    case refund
}

/// Provides the reason for why the refund is pending. Possible values are: `processing`,
/// `insufficient_funds`, or `charge_pending`.
public enum RefundPendingReason: String, Codable, Sendable {
    case chargePending = "charge_pending"
    case insufficientFunds = "insufficient_funds"
    case processing
}

/// Reason for the refund, which is either user-provided (`duplicate`, `fraudulent`, or
/// `requested_by_customer`) or generated by Stripe internally (`expired_uncaptured_charge`).
public enum RefundReason: String, Codable, Sendable {
    case duplicate
    case expiredUncapturedCharge = "expired_uncaptured_charge"
    case fraudulent
    case requestedByCustomer = "requested_by_customer"
}

public struct RefundList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Refund]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

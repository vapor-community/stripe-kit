//
//  Price.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// Prices define the unit cost, currency, and (optional) billing cycle for both recurring and one-time
/// purchases of products. [Products](https://api.stripe.com#products) help you track inventory or
/// provisioning, and prices help you track payment terms. Different physical goods or levels of service
/// should be represented by products, and pricing options should be represented by prices. This
/// approach lets you change prices without having to change your provisioning scheme. For example, you
/// might have a single "gold" product that has prices for $10/month, $100/year, and €9 once. Related
/// guides: [Set up a subscription](https://docs.stripe.com/billing/subscriptions/set-up-subscription),
/// [create an invoice](https://docs.stripe.com/billing/invoices/create), and more about [products and
/// prices](https://docs.stripe.com/products-prices/overview).
public struct Price: Codable, Sendable {
    /// Whether the price can be used for new purchases.
    public var active: Bool
    /// Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates
    /// that the fixed amount (specified in `unit_amount` or `unit_amount_decimal`) will be charged per unit
    /// in `quantity` (for prices with `usage_type=licensed`), or per unit of total usage (for prices with
    /// `usage_type=metered`). `tiered` indicates that the unit pricing will be computed using a tiering
    /// strategy as defined using the `tiers` and `tiers_mode` attributes.
    public var billingScheme: PriceBillingScheme
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// Prices defined in each available currency option. Each key must be a three-letter [ISO currency
    /// code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported
    /// currency](https://stripe.com/docs/currencies).
    public var currencyOptions: [String: CurrencyOption]?
    /// When set, provides configuration for the amount to be adjusted by the customer during Checkout
    /// Sessions and Payment Links.
    @Expandable<CustomUnitAmount>
    public var customUnitAmount: String?
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// A lookup key used to retrieve prices dynamically from a static string. This may be up to 200
    /// characters.
    public var lookupKey: String?
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]
    /// A brief description of the price, hidden from customers.
    public var nickname: String?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: PriceObject
    /// The ID of the product this price is associated with.
    @Expandable<Product>
    public var product: String?
    /// The recurring components of a price such as `interval` and `usage_type`.
    @Expandable<Recurring>
    public var recurring: String?
    /// Only required if a [default tax
    /// behavior](https://docs.stripe.com/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended))
    /// was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of
    /// taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as
    /// either `inclusive` or `exclusive`, it cannot be changed.
    public var taxBehavior: PriceTaxBehavior?
    /// Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to
    /// `tiered`. See also the documentation for `billing_scheme`.
    @ExpandableCollection<PriceTier>
    public var tiers: [String]?
    /// Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the
    /// maximum quantity within a period determines the per unit price. In `graduated` tiering, pricing can
    /// change as the quantity grows.
    public var tiersMode: PriceTiersMode?
    /// Apply a transformation to the reported usage or set quantity before computing the amount billed.
    /// Cannot be combined with `tiers`.
    @Expandable<TransformQuantity>
    public var transformQuantity: String?
    /// One of `one_time` or `recurring` depending on whether the price is for a one-time purchase or a
    /// recurring (subscription) purchase.
    public var type: PriceType
    /// The unit amount in cents (or local equivalent) to be charged, represented as a whole integer if
    /// possible. Only set if `billing_scheme=per_unit`.
    public var unitAmount: Int?
    /// The unit amount in cents (or local equivalent) to be charged, represented as a decimal string with
    /// at most 12 decimal places. Only set if `billing_scheme=per_unit`.
    public var unitAmountDecimal: String?

    public enum CodingKeys: String, CodingKey {
        case active
        case billingScheme = "billing_scheme"
        case created
        case currency
        case currencyOptions = "currency_options"
        case customUnitAmount = "custom_unit_amount"
        case id
        case livemode
        case lookupKey = "lookup_key"
        case metadata
        case nickname
        case object
        case product
        case recurring
        case taxBehavior = "tax_behavior"
        case tiers
        case tiersMode = "tiers_mode"
        case transformQuantity = "transform_quantity"
        case type
        case unitAmount = "unit_amount"
        case unitAmountDecimal = "unit_amount_decimal"
    }
}

/// Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates
/// that the fixed amount (specified in `unit_amount` or `unit_amount_decimal`) will be charged per unit
/// in `quantity` (for prices with `usage_type=licensed`), or per unit of total usage (for prices with
/// `usage_type=metered`). `tiered` indicates that the unit pricing will be computed using a tiering
/// strategy as defined using the `tiers` and `tiers_mode` attributes.
public enum PriceBillingScheme: String, Codable, Sendable {
    case perUnit = "per_unit"
    case tiered
}

/// String representing the object's type. Objects of the same type share the same value.
public enum PriceObject: String, Codable, Sendable {
    case price
}

/// Only required if a [default tax
/// behavior](https://docs.stripe.com/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended))
/// was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of
/// taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as
/// either `inclusive` or `exclusive`, it cannot be changed.
public enum PriceTaxBehavior: String, Codable, Sendable {
    case exclusive
    case inclusive
    case unspecified
}

/// Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the
/// maximum quantity within a period determines the per unit price. In `graduated` tiering, pricing can
/// change as the quantity grows.
public enum PriceTiersMode: String, Codable, Sendable {
    case graduated
    case volume
}

/// One of `one_time` or `recurring` depending on whether the price is for a one-time purchase or a
/// recurring (subscription) purchase.
public enum PriceType: String, Codable, Sendable {
    case oneTime = "one_time"
    case recurring
}

public struct PriceList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Price]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

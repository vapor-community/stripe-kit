//
//  TransferReversal.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// [Stripe Connect](https://docs.stripe.com/connect) platforms can reverse transfers made to a
/// connected account, either entirely or partially, and can also specify whether to refund any related
/// application fees. Transfer reversals add to the platform's balance and subtract from the destination
/// account's balance. Reversing a transfer that was made for a [destination
/// charge](/docs/connect/destination-charges) is allowed only up to the amount of the charge. It is
/// possible to reverse a
/// [transfer_group](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-options)
/// transfer only if the destination account has enough balance to cover the reversal. Related guide:
/// [Reverse
/// transfers](https://docs.stripe.com/connect/separate-charges-and-transfers#reverse-transfers)
public struct TransferReversal: Codable, Sendable {
    /// Amount, in cents (or local equivalent).
    public var amount: Int
    /// Balance transaction that describes the impact on your account balance.
    @Expandable<BalanceTransaction>
    public var balanceTransaction: String?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// Linked payment refund for the transfer reversal.
    @Expandable<Refund>
    public var destinationPaymentRefund: String?
    /// Unique identifier for the object.
    public var id: String
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: TransferReversalObject
    /// ID of the refund responsible for the transfer reversal.
    @Expandable<Refund>
    public var sourceRefund: String?
    /// ID of the transfer that was reversed.
    @Expandable<Transfer>
    public var transfer: String?

    public enum CodingKeys: String, CodingKey {
        case amount
        case balanceTransaction = "balance_transaction"
        case created
        case currency
        case destinationPaymentRefund = "destination_payment_refund"
        case id
        case metadata
        case object
        case sourceRefund = "source_refund"
        case transfer
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum TransferReversalObject: String, Codable, Sendable {
    case transferReversal = "transfer_reversal"
}

public struct TransferReversalList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [TransferReversal]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

//
//  Payout.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// A `Payout` object is created when you receive funds from Stripe, or when you initiate a payout to
/// either a bank account or debit card of a [connected Stripe
/// account](/docs/connect/bank-debit-card-payouts). You can retrieve individual payouts, and list all
/// payouts. Payouts are made on [varying schedules](/docs/connect/manage-payout-schedule), depending on
/// your country and industry. Related guide: [Receiving payouts](https://docs.stripe.com/payouts)
public struct Payout: Codable, Sendable {
    /// The amount (in cents (or local equivalent)) that transfers to your bank account or debit card.
    public var amount: Int
    /// The application fee (if any) for the payout. [See the Connect
    /// documentation](https://docs.stripe.com/connect/instant-payouts#monetization-and-fees) for details.
    @Expandable<ApplicationFee>
    public var applicationFee: String?
    /// The amount of the application fee (if any) requested for the payout. [See the Connect
    /// documentation](https://docs.stripe.com/connect/instant-payouts#monetization-and-fees) for details.
    public var applicationFeeAmount: Int?
    /// Date that you can expect the payout to arrive in the bank. This factors in delays to account for
    /// weekends or bank holidays.
    public var arrivalDate: Int
    /// Returns `true` if the payout is created by an [automated payout
    /// schedule](https://docs.stripe.com/payouts#payout-schedule) and `false` if it's [requested
    /// manually](https://stripe.com/docs/payouts#manual-payouts).
    public var automatic: Bool
    /// ID of the balance transaction that describes the impact of this payout on your account balance.
    @Expandable<BalanceTransaction>
    public var balanceTransaction: String?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String?
    /// ID of the bank account or card the payout is sent to.
    @Expandable<ExternalAccount>
    public var destination: String?
    /// If the payout fails or cancels, this is the ID of the balance transaction that reverses the initial
    /// balance transaction and returns the funds from the failed payout back in your balance.
    @Expandable<BalanceTransaction>
    public var failureBalanceTransaction: String?
    /// Error code that provides a reason for a payout failure, if available. View our [list of failure
    /// codes](https://docs.stripe.com/api#payout_failures).
    public var failureCode: String?
    /// Message that provides the reason for a payout failure, if available.
    public var failureMessage: String?
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]?
    /// The method used to send this payout, which can be `standard` or `instant`. `instant` is supported
    /// for payouts to debit cards and bank accounts in certain countries. Learn more about [bank support
    /// for Instant Payouts](https://stripe.com/docs/payouts/instant-payouts-banks).
    public var method: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: PayoutObject
    /// If the payout reverses another, this is the ID of the original payout.
    @Expandable<Payout>
    public var originalPayout: String?
    /// ID of the v2 FinancialAccount the funds are sent to.
    public var payoutMethod: String?
    /// If `completed`, you can use the [Balance Transactions
    /// API](https://docs.stripe.com/api/balance_transactions/list#balance_transaction_list-payout) to list
    /// all balance transactions that are paid out in this payout.
    public var reconciliationStatus: PayoutReconciliationStatus
    /// If the payout reverses, this is the ID of the payout that reverses this payout.
    @Expandable<Payout>
    public var reversedBy: String?
    /// The source balance this payout came from, which can be one of the following: `card`, `fpx`, or
    /// `bank_account`.
    public var sourceType: String
    /// Extra information about a payout that displays on the user's bank statement.
    public var statementDescriptor: String?
    /// Current status of the payout: `paid`, `pending`, `in_transit`, `canceled` or `failed`. A payout is
    /// `pending` until it's submitted to the bank, when it becomes `in_transit`. The status changes to
    /// `paid` if the transaction succeeds, or to `failed` or `canceled` (within 5 business days). Some
    /// payouts that fail might initially show as `paid`, then change to `failed`.
    public var status: String
    /// A value that generates from the beneficiary's bank that allows users to track payouts with their
    /// bank. Banks might call this a "reference number" or something similar.
    @Expandable<PayoutsTraceId>
    public var traceId: String?
    /// Can be `bank_account` or `card`.
    public var type: PayoutType

    public enum CodingKeys: String, CodingKey {
        case amount
        case applicationFee = "application_fee"
        case applicationFeeAmount = "application_fee_amount"
        case arrivalDate = "arrival_date"
        case automatic
        case balanceTransaction = "balance_transaction"
        case created
        case currency
        case description
        case destination
        case failureBalanceTransaction = "failure_balance_transaction"
        case failureCode = "failure_code"
        case failureMessage = "failure_message"
        case id
        case livemode
        case metadata
        case method
        case object
        case originalPayout = "original_payout"
        case payoutMethod = "payout_method"
        case reconciliationStatus = "reconciliation_status"
        case reversedBy = "reversed_by"
        case sourceType = "source_type"
        case statementDescriptor = "statement_descriptor"
        case status
        case traceId = "trace_id"
        case type
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum PayoutObject: String, Codable, Sendable {
    case payout
}

/// If `completed`, you can use the [Balance Transactions
/// API](https://docs.stripe.com/api/balance_transactions/list#balance_transaction_list-payout) to list
/// all balance transactions that are paid out in this payout.
public enum PayoutReconciliationStatus: String, Codable, Sendable {
    case completed
    case inProgress = "in_progress"
    case notApplicable = "not_applicable"
}

/// Can be `bank_account` or `card`.
public enum PayoutType: String, Codable, Sendable {
    case bankAccount = "bank_account"
    case card
}

public struct PayoutList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Payout]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

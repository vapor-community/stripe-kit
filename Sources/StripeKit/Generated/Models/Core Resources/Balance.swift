//
//  Balance.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// This is an object representing your Stripe balance. You can retrieve it to see the balance currently
/// on your Stripe account. The top-level `available` and `pending` comprise your "payments balance."
/// Related guide: [Balances and settlement time](https://docs.stripe.com/payments/balances),
/// [Understanding Connect account balances](https://docs.stripe.com/connect/account-balances)
public struct Balance: Codable, Sendable {
    /// Available funds that you can transfer or pay out automatically by Stripe or explicitly through the
    /// [Transfers API](https://api.stripe.com#transfers) or [Payouts API](https://api.stripe.com#payouts).
    /// You can find the available balance for each currency and payment type in the `source_types`
    /// property.
    @ExpandableCollection<BalanceAmount>
    public var available: [String]?
    /// Funds held due to negative balances on connected accounts where
    /// [account.controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection)
    /// is `application`, which includes Custom accounts. You can find the connect reserve balance for each
    /// currency and payment type in the `source_types` property.
    @ExpandableCollection<BalanceAmount>
    public var connectReserved: [String]?
    /// Funds that you can pay out using Instant Payouts.
    @ExpandableCollection<BalanceAmountNet>
    public var instantAvailable: [String]?
    @Expandable<BalanceDetail>
    public var issuing: String?
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: BalanceObject
    /// Funds that aren't available in the balance yet. You can find the pending balance for each currency
    /// and each payment type in the `source_types` property.
    @ExpandableCollection<BalanceAmount>
    public var pending: [String]?
    @Expandable<BalanceDetailUngated>
    public var refundAndDisputePrefunding: String?

    public enum CodingKeys: String, CodingKey {
        case available
        case connectReserved = "connect_reserved"
        case instantAvailable = "instant_available"
        case issuing
        case livemode
        case object
        case pending
        case refundAndDisputePrefunding = "refund_and_dispute_prefunding"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum BalanceObject: String, Codable, Sendable {
    case balance
}

public struct BalanceList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Balance]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

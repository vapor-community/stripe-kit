//
//  CustomerBalanceTransaction.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// Each customer has a [Balance](https://docs.stripe.com/api/customers/object#customer_object-balance)
/// value, which denotes a debit or credit that's automatically applied to their next invoice upon
/// finalization. You may modify the value directly by using the [update customer
/// API](https://docs.stripe.com/api/customers/update), or by creating a Customer Balance Transaction,
/// which increments or decrements the customer's `balance` by the specified `amount`. Related guide:
/// [Customer balance](https://docs.stripe.com/billing/customer/balance)
public struct CustomerBalanceTransaction: Codable, Sendable {
    /// The amount of the transaction. A negative value is a credit for the customer's balance, and a
    /// positive value is a debit to the customer's `balance`.
    public var amount: Int
    /// The ID of the checkout session (if any) that created the transaction.
    @Expandable<CheckoutSession>
    public var checkoutSession: String?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// The ID of the credit note (if any) related to the transaction.
    @Expandable<CreditNote>
    public var creditNote: String?
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// The ID of the customer the transaction belongs to.
    @Expandable<Customer>
    public var customer: String?
    /// The ID of an Account representing a customer that the transaction belongs to.
    public var customerAccount: String?
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String?
    /// The customer's `balance` after the transaction was applied. A negative value decreases the amount
    /// due on the customer's next invoice. A positive value increases the amount due on the customer's next
    /// invoice.
    public var endingBalance: Int
    /// Unique identifier for the object.
    public var id: String
    /// The ID of the invoice (if any) related to the transaction.
    @Expandable<Invoice>
    public var invoice: String?
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: CustomerBalanceTransactionObject
    /// Transaction type: `adjustment`, `applied_to_invoice`, `credit_note`, `initial`, `invoice_overpaid`,
    /// `invoice_too_large`, `invoice_too_small`, `unspent_receiver_credit`, `unapplied_from_invoice`,
    /// `checkout_session_subscription_payment`, or `checkout_session_subscription_payment_canceled`. See
    /// the [Customer Balance page](https://docs.stripe.com/billing/customer/balance#types) to learn more
    /// about transaction types.
    public var type: CustomerBalanceTransactionType

    public enum CodingKeys: String, CodingKey {
        case amount
        case checkoutSession = "checkout_session"
        case created
        case creditNote = "credit_note"
        case currency
        case customer
        case customerAccount = "customer_account"
        case description
        case endingBalance = "ending_balance"
        case id
        case invoice
        case livemode
        case metadata
        case object
        case type
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum CustomerBalanceTransactionObject: String, Codable, Sendable {
    case customerBalanceTransaction = "customer_balance_transaction"
}

/// Transaction type: `adjustment`, `applied_to_invoice`, `credit_note`, `initial`, `invoice_overpaid`,
/// `invoice_too_large`, `invoice_too_small`, `unspent_receiver_credit`, `unapplied_from_invoice`,
/// `checkout_session_subscription_payment`, or `checkout_session_subscription_payment_canceled`. See
/// the [Customer Balance page](https://docs.stripe.com/billing/customer/balance#types) to learn more
/// about transaction types.
public enum CustomerBalanceTransactionType: String, Codable, Sendable {
    case adjustment
    case appliedToInvoice = "applied_to_invoice"
    case checkoutSessionSubscriptionPayment = "checkout_session_subscription_payment"
    case checkoutSessionSubscriptionPaymentCanceled = "checkout_session_subscription_payment_canceled"
    case creditNote = "credit_note"
    case initial
    case invoiceOverpaid = "invoice_overpaid"
    case invoiceTooLarge = "invoice_too_large"
    case invoiceTooSmall = "invoice_too_small"
    case migration
    case unappliedFromInvoice = "unapplied_from_invoice"
    case unspentReceiverCredit = "unspent_receiver_credit"
}

public struct CustomerBalanceTransactionList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [CustomerBalanceTransaction]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

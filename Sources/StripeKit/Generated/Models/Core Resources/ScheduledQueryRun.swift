//
//  ScheduledQueryRun.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// If you have [scheduled a Sigma query](https://docs.stripe.com/sigma/scheduled-queries), you'll
/// receive a `sigma.scheduled_query_run.created` webhook each time the query runs. The webhook contains
/// a `ScheduledQueryRun` object, which you can use to retrieve the query results.
public struct ScheduledQueryRun: Codable, Sendable {
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// When the query was run, Sigma contained a snapshot of your Stripe data at this time.
    public var dataLoadTime: Int
    @Expandable<SigmaScheduledQueryRunError>
    public var error: String?
    /// The file object representing the results of the query.
    @Expandable<File>
    public var file: String?
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: ScheduledQueryRunObject
    /// Time at which the result expires and is no longer available for download.
    public var resultAvailableUntil: Int
    /// SQL for the query.
    public var sql: String
    /// The query's execution status, which will be `completed` for successful runs, and `canceled`,
    /// `failed`, or `timed_out` otherwise.
    public var status: String
    /// Title of the query.
    public var title: String

    public enum CodingKeys: String, CodingKey {
        case created
        case dataLoadTime = "data_load_time"
        case error
        case file
        case id
        case livemode
        case object
        case resultAvailableUntil = "result_available_until"
        case sql
        case status
        case title
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum ScheduledQueryRunObject: String, Codable, Sendable {
    case scheduledQueryRun = "scheduled_query_run"
}

public struct ScheduledQueryRunList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [ScheduledQueryRun]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

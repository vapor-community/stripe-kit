//
//  CreditNote.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// Issue a credit note to adjust an invoice's amount after the invoice is finalized. Related guide:
/// [Credit notes](https://docs.stripe.com/billing/invoices/credit-notes)
public struct CreditNote: Codable, Sendable {
    /// The integer amount in cents (or local equivalent) representing the total amount of the credit note,
    /// including tax.
    public var amount: Int
    /// This is the sum of all the shipping amounts.
    public var amountShipping: Int
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// ID of the customer.
    @Expandable<Customer>
    public var customer: String?
    /// ID of the account representing the customer.
    public var customerAccount: String?
    /// Customer balance transaction related to this credit note.
    @Expandable<CustomerBalanceTransaction>
    public var customerBalanceTransaction: String?
    /// The integer amount in cents (or local equivalent) representing the total amount of discount that was
    /// credited.
    public var discountAmount: Int
    /// The aggregate amounts calculated per discount for all line items.
    @ExpandableCollection<DiscountsResourceDiscountAmount>
    public var discountAmounts: [String]?
    /// The date when this credit note is in effect. Same as `created` unless overwritten. When defined,
    /// this value replaces the system-generated 'Date of issue' printed on the credit note PDF.
    public var effectiveAt: Int?
    /// Unique identifier for the object.
    public var id: String
    /// ID of the invoice.
    @Expandable<Invoice>
    public var invoice: String?
    /// Line items that make up the credit note
    public var lines: CreditNoteLineItemList
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Customer-facing text that appears on the credit note PDF.
    public var memo: String?
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]?
    /// A unique number that identifies this particular credit note and appears on the PDF of the credit
    /// note and its associated invoice.
    public var number: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: CreditNoteObject
    /// Amount that was credited outside of Stripe.
    public var outOfBandAmount: Int?
    /// The link to download the PDF of the credit note.
    public var pdf: String
    /// The amount of the credit note that was refunded to the customer, credited to the customer's balance,
    /// credited outside of Stripe, or any combination thereof.
    public var postPaymentAmount: Int
    /// The amount of the credit note by which the invoice's `amount_remaining` and `amount_due` were
    /// reduced.
    public var prePaymentAmount: Int
    /// The pretax credit amounts (ex: discount, credit grants, etc) for all line items.
    @ExpandableCollection<CreditNotesPretaxCreditAmount>
    public var pretaxCreditAmounts: [String]?
    /// Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or
    /// `product_unsatisfactory`
    public var reason: CreditNoteReason?
    /// Refunds related to this credit note.
    @ExpandableCollection<CreditNoteRefund>
    public var refunds: [String]?
    /// The details of the cost of shipping, including the ShippingRate applied to the invoice.
    @Expandable<InvoicesResourceShippingCost>
    public var shippingCost: String?
    /// Status of this credit note, one of `issued` or `void`. Learn more about [voiding credit
    /// notes](https://docs.stripe.com/billing/invoices/credit-notes#voiding).
    public var status: CreditNoteStatus
    /// The integer amount in cents (or local equivalent) representing the amount of the credit note,
    /// excluding exclusive tax and invoice level discounts.
    public var subtotal: Int
    /// The integer amount in cents (or local equivalent) representing the amount of the credit note,
    /// excluding all tax and invoice level discounts.
    public var subtotalExcludingTax: Int?
    /// The integer amount in cents (or local equivalent) representing the total amount of the credit note,
    /// including tax and all discount.
    public var total: Int
    /// The integer amount in cents (or local equivalent) representing the total amount of the credit note,
    /// excluding tax, but including discounts.
    public var totalExcludingTax: Int?
    /// The aggregate tax information for all line items.
    @ExpandableCollection<BillingBillResourceInvoicingTaxesTax>
    public var totalTaxes: [String]?
    /// Type of this credit note, one of `pre_payment` or `post_payment`. A `pre_payment` credit note means
    /// it was issued when the invoice was open. A `post_payment` credit note means it was issued when the
    /// invoice was paid.
    public var type: CreditNoteType
    /// The time that the credit note was voided.
    public var voidedAt: Int?

    public enum CodingKeys: String, CodingKey {
        case amount
        case amountShipping = "amount_shipping"
        case created
        case currency
        case customer
        case customerAccount = "customer_account"
        case customerBalanceTransaction = "customer_balance_transaction"
        case discountAmount = "discount_amount"
        case discountAmounts = "discount_amounts"
        case effectiveAt = "effective_at"
        case id
        case invoice
        case lines
        case livemode
        case memo
        case metadata
        case number
        case object
        case outOfBandAmount = "out_of_band_amount"
        case pdf
        case postPaymentAmount = "post_payment_amount"
        case prePaymentAmount = "pre_payment_amount"
        case pretaxCreditAmounts = "pretax_credit_amounts"
        case reason
        case refunds
        case shippingCost = "shipping_cost"
        case status
        case subtotal
        case subtotalExcludingTax = "subtotal_excluding_tax"
        case total
        case totalExcludingTax = "total_excluding_tax"
        case totalTaxes = "total_taxes"
        case type
        case voidedAt = "voided_at"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum CreditNoteObject: String, Codable, Sendable {
    case creditNote = "credit_note"
}

/// Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or
/// `product_unsatisfactory`
public enum CreditNoteReason: String, Codable, Sendable {
    case duplicate
    case fraudulent
    case orderChange = "order_change"
    case productUnsatisfactory = "product_unsatisfactory"
}

/// Status of this credit note, one of `issued` or `void`. Learn more about [voiding credit
/// notes](https://docs.stripe.com/billing/invoices/credit-notes#voiding).
public enum CreditNoteStatus: String, Codable, Sendable {
    case issued
    case void
}

/// Type of this credit note, one of `pre_payment` or `post_payment`. A `pre_payment` credit note means
/// it was issued when the invoice was open. A `post_payment` credit note means it was issued when the
/// invoice was paid.
public enum CreditNoteType: String, Codable, Sendable {
    case mixed
    case postPayment = "post_payment"
    case prePayment = "pre_payment"
}

public struct CreditNoteList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [CreditNote]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

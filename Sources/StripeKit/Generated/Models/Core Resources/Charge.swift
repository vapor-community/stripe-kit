//
//  Charge.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// The `Charge` object represents a single attempt to move money into your Stripe account.
/// PaymentIntent confirmation is the most common way to create Charges, but [Account
/// Debits](https://docs.stripe.com/connect/account-debits) may also create Charges. Some legacy payment
/// flows create Charges directly, which is not recommended for new integrations.
public struct Charge: Codable, Sendable {
    /// Amount intended to be collected by this payment. A positive integer representing how much to charge
    /// in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal) (e.g., 100 cents to
    /// charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum amount is $0.50 US or
    /// [equivalent in charge
    /// currency](https://docs.stripe.com/currencies#minimum-and-maximum-charge-amounts). The amount value
    /// supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99).
    public var amount: Int
    /// Amount in cents (or local equivalent) captured (can be less than the amount attribute on the charge
    /// if a partial capture was made).
    public var amountCaptured: Int
    /// Amount in cents (or local equivalent) refunded (can be less than the amount attribute on the charge
    /// if a partial refund was issued).
    public var amountRefunded: Int
    /// ID of the Connect application that created the charge.
    @Expandable<Application>
    public var application: String?
    /// The application fee (if any) for the charge. [See the Connect
    /// documentation](https://docs.stripe.com/connect/direct-charges#collect-fees) for details.
    @Expandable<ApplicationFee>
    public var applicationFee: String?
    /// The amount of the application fee (if any) requested for the charge. [See the Connect
    /// documentation](https://docs.stripe.com/connect/direct-charges#collect-fees) for details.
    public var applicationFeeAmount: Int?
    /// Authorization code on the charge.
    public var authorizationCode: String?
    /// ID of the balance transaction that describes the impact of this charge on your account balance (not
    /// including refunds or disputes).
    @Expandable<BalanceTransaction>
    public var balanceTransaction: String?
    @Expandable<BillingDetails>
    public var billingDetails: String?
    /// The full statement descriptor that is passed to card networks, and that is displayed on your
    /// customers' credit card and bank statements. Allows you to see what the statement descriptor looks
    /// like after the static and dynamic portions are combined. This value only exists for card payments.
    public var calculatedStatementDescriptor: String?
    /// If the charge was created without capturing, this Boolean represents whether it is still uncaptured
    /// or has since been captured.
    public var captured: Bool
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// ID of the customer this charge is for if one exists.
    @Expandable<Customer>
    public var customer: String?
    /// An arbitrary string attached to the object. Often useful for displaying to users.
    public var description: String?
    /// Whether the charge has been disputed.
    public var disputed: Bool
    /// ID of the balance transaction that describes the reversal of the balance on your account due to
    /// payment failure.
    @Expandable<BalanceTransaction>
    public var failureBalanceTransaction: String?
    /// Error code explaining reason for charge failure if available (see [the errors
    /// section](https://docs.stripe.com/error-codes) for a list of codes).
    public var failureCode: String?
    /// Message to user further explaining reason for charge failure if available.
    public var failureMessage: String?
    /// Information on fraud assessments for the charge.
    @Expandable<ChargeFraudDetails>
    public var fraudDetails: String?
    /// Unique identifier for the object.
    public var id: String
    @Expandable<Level3>
    public var level3: String?
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object.
    /// This can be useful for storing additional information about the object in a structured format.
    public var metadata: [String: String]
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: ChargeObject
    /// The account (if any) the charge was made on behalf of without triggering an automatic transfer. See
    /// the [Connect documentation](https://docs.stripe.com/connect/separate-charges-and-transfers) for
    /// details.
    @Expandable<Account>
    public var onBehalfOf: String?
    /// Details about whether the payment was accepted, and why. See [understanding
    /// declines](https://docs.stripe.com/declines) for details.
    @Expandable<ChargeOutcome>
    public var outcome: String?
    /// `true` if the charge succeeded, or was successfully authorized for later capture.
    public var paid: Bool
    /// ID of the PaymentIntent associated with this charge, if one exists.
    @Expandable<PaymentIntent>
    public var paymentIntent: String?
    /// ID of the payment method used in this charge.
    public var paymentMethod: String?
    /// Details about the payment method at the time of the transaction.
    @Expandable<PaymentMethodDetails>
    public var paymentMethodDetails: String?
    @Expandable<PaymentFlowsPaymentIntentPresentmentDetails>
    public var presentmentDetails: String?
    @Expandable<RadarRadarOptions>
    public var radarOptions: String?
    /// This is the email address that the receipt for this charge was sent to.
    public var receiptEmail: String?
    /// This is the transaction number that appears on email receipts sent for this charge. This attribute
    /// will be `null` until a receipt has been sent.
    public var receiptNumber: String?
    /// This is the URL to view the receipt for this charge. The receipt is kept up-to-date to the latest
    /// state of the charge, including any refunds. If the charge is for an Invoice, the receipt will be
    /// stylized as an Invoice receipt.
    public var receiptUrl: String?
    /// Whether the charge has been fully refunded. If the charge is only partially refunded, this attribute
    /// will still be false.
    public var refunded: Bool
    /// A list of refunds that have been applied to the charge.
    public var refunds: RefundList?
    /// ID of the review associated with this charge if one exists.
    @Expandable<Review>
    public var review: String?
    /// Shipping information for the charge.
    @Expandable<Shipping>
    public var shipping: String?
    /// This is a legacy field that will be removed in the future. It contains the Source, Card, or
    /// BankAccount object used for the charge. For details about the payment method used for this charge,
    /// refer to `payment_method` or `payment_method_details` instead.
    @Expandable<PaymentSource>
    public var source: String?
    /// The transfer ID which created this charge. Only present if the charge came from another Stripe
    /// account. [See the Connect documentation](https://docs.stripe.com/connect/destination-charges) for
    /// details.
    @Expandable<Transfer>
    public var sourceTransfer: String?
    /// For a non-card charge, text that appears on the customer's statement as the statement descriptor.
    /// This value overrides the account's default statement descriptor. For information about requirements,
    /// including the 22-character limit, see [the Statement Descriptor
    /// docs](https://docs.stripe.com/get-started/account/statement-descriptors). For a card charge, this
    /// value is ignored unless you don't specify a `statement_descriptor_suffix`, in which case this value
    /// is used as the suffix.
    public var statementDescriptor: String?
    /// Provides information about a card charge. Concatenated to the account's [statement descriptor
    /// prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the
    /// complete statement descriptor that appears on the customer's statement. If the account has no prefix
    /// value, the suffix is concatenated to the account's statement descriptor.
    public var statementDescriptorSuffix: String?
    /// The status of the payment is either `succeeded`, `pending`, or `failed`.
    public var status: ChargeStatus
    /// ID of the transfer to the `destination` account (only applicable if the charge was created using the
    /// `destination` parameter).
    @Expandable<Transfer>
    public var transfer: String?
    /// An optional dictionary including the account to automatically transfer to as part of a destination
    /// charge. [See the Connect documentation](https://docs.stripe.com/connect/destination-charges) for
    /// details.
    @Expandable<ChargeTransferData>
    public var transferData: String?
    /// A string that identifies this transaction as part of a group. See the [Connect
    /// documentation](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-options) for
    /// details.
    public var transferGroup: String?

    public enum CodingKeys: String, CodingKey {
        case amount
        case amountCaptured = "amount_captured"
        case amountRefunded = "amount_refunded"
        case application
        case applicationFee = "application_fee"
        case applicationFeeAmount = "application_fee_amount"
        case authorizationCode = "authorization_code"
        case balanceTransaction = "balance_transaction"
        case billingDetails = "billing_details"
        case calculatedStatementDescriptor = "calculated_statement_descriptor"
        case captured
        case created
        case currency
        case customer
        case description
        case disputed
        case failureBalanceTransaction = "failure_balance_transaction"
        case failureCode = "failure_code"
        case failureMessage = "failure_message"
        case fraudDetails = "fraud_details"
        case id
        case level3
        case livemode
        case metadata
        case object
        case onBehalfOf = "on_behalf_of"
        case outcome
        case paid
        case paymentIntent = "payment_intent"
        case paymentMethod = "payment_method"
        case paymentMethodDetails = "payment_method_details"
        case presentmentDetails = "presentment_details"
        case radarOptions = "radar_options"
        case receiptEmail = "receipt_email"
        case receiptNumber = "receipt_number"
        case receiptUrl = "receipt_url"
        case refunded
        case refunds
        case review
        case shipping
        case source
        case sourceTransfer = "source_transfer"
        case statementDescriptor = "statement_descriptor"
        case statementDescriptorSuffix = "statement_descriptor_suffix"
        case status
        case transfer
        case transferData = "transfer_data"
        case transferGroup = "transfer_group"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum ChargeObject: String, Codable, Sendable {
    case charge
}

/// The status of the payment is either `succeeded`, `pending`, or `failed`.
public enum ChargeStatus: String, Codable, Sendable {
    case failed
    case pending
    case succeeded
}

public struct ChargeList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Charge]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

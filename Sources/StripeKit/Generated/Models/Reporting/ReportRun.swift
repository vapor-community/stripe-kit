//
//  ReportRun.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// The Report Run object represents an instance of a report type generated with specific run
/// parameters. Once the object is created, Stripe begins processing the report. When the report has
/// finished running, it will give you a reference to a file where you can retrieve your results. For an
/// overview, see [API Access to Reports](https://docs.stripe.com/reporting/statements/api). Note that
/// certain report types can only be run based on your live-mode data (not test-mode data), and will
/// error when queried without a [live-mode API key](https://docs.stripe.com/keys#test-live-modes).
public struct ReportRun: Codable, Sendable {
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// If something should go wrong during the run, a message about the failure (populated when
    /// `status=failed`).
    public var error: String?
    /// Unique identifier for the object.
    public var id: String
    /// `true` if the report is run on live mode data and `false` if it is run on test mode data.
    public var livemode: Bool
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: ReportRunObject
    @Expandable<FinancialReportingFinanceReportRunRunParameters>
    public var parameters: String?
    /// The ID of the [report type](https://docs.stripe.com/reports/report-types) to run, such as
    /// `"balance.summary.1"`.
    public var reportType: String
    /// The file object representing the result of the report run (populated when `status=succeeded`).
    @Expandable<File>
    public var result: String?
    /// Status of this report run. This will be `pending` when the run is initially created. When the run
    /// finishes, this will be set to `succeeded` and the `result` field will be populated. Rarely, we may
    /// encounter an error, at which point this will be set to `failed` and the `error` field will be
    /// populated.
    public var status: String
    /// Timestamp at which this run successfully finished (populated when `status=succeeded`). Measured in
    /// seconds since the Unix epoch.
    public var succeededAt: Int?

    public enum CodingKeys: String, CodingKey {
        case created
        case error
        case id
        case livemode
        case object
        case parameters
        case reportType = "report_type"
        case result
        case status
        case succeededAt = "succeeded_at"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum ReportRunObject: String, Codable, Sendable {
    case reportingReportRun = "reporting.report_run"
}

public struct ReportRunList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [ReportRun]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

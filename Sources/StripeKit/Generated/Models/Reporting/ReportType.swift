//
//  ReportType.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// The Report Type resource corresponds to a particular type of report, such as the "Activity summary"
/// or "Itemized payouts" reports. These objects are identified by an ID belonging to a set of
/// enumerated values. See [API Access to Reports
/// documentation](https://docs.stripe.com/reporting/statements/api) for those Report Type IDs, along
/// with required and optional parameters. Note that certain report types can only be run based on your
/// live-mode data (not test-mode data), and will error when queried without a [live-mode API
/// key](https://docs.stripe.com/keys#test-live-modes).
public struct ReportType: Codable, Sendable {
    /// Most recent time for which this Report Type is available. Measured in seconds since the Unix epoch.
    public var dataAvailableEnd: Int
    /// Earliest time for which this Report Type is available. Measured in seconds since the Unix epoch.
    public var dataAvailableStart: Int
    /// List of column names that are included by default when this Report Type gets run. (If the Report
    /// Type doesn't support the `columns` parameter, this will be null.)
    public var defaultColumns: [String]?
    /// The [ID of the Report
    /// Type](https://docs.stripe.com/reporting/statements/api#available-report-types), such as
    /// `balance.summary.1`.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// Human-readable name of the Report Type
    public var name: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: ReportTypeObject
    /// When this Report Type was latest updated. Measured in seconds since the Unix epoch.
    public var updated: Int
    /// Version of the Report Type. Different versions report with the same ID will have the same purpose,
    /// but may take different run parameters or have different result schemas.
    public var version: Int

    public enum CodingKeys: String, CodingKey {
        case dataAvailableEnd = "data_available_end"
        case dataAvailableStart = "data_available_start"
        case defaultColumns = "default_columns"
        case id
        case livemode
        case name
        case object
        case updated
        case version
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum ReportTypeObject: String, Codable, Sendable {
    case reportingReportType = "reporting.report_type"
}

public struct ReportTypeList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [ReportType]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

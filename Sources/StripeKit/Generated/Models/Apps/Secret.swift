//
//  Secret.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// Secret Store is an API that allows Stripe Apps developers to securely persist secrets for use by UI
/// Extensions and app backends. The primary resource in Secret Store is a `secret`. Other apps can't
/// view secrets created by an app. Additionally, secrets are scoped to provide further permission
/// control. All Dashboard users and the app backend share `account` scoped secrets. Use the `account`
/// scope for secrets that don't change per-user, like a third-party API key. A `user` scoped secret is
/// accessible by the app backend and one specific Dashboard user. Use the `user` scope for per-user
/// secrets like per-user OAuth tokens, where different users might have different permissions. Related
/// guide: [Store data between page
/// reloads](https://docs.stripe.com/stripe-apps/store-auth-data-custom-objects)
public struct Secret: Codable, Sendable {
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// If true, indicates that this secret has been deleted
    public var deleted: Bool?
    /// The Unix timestamp for the expiry time of the secret, after which the secret deletes.
    public var expiresAt: Int?
    /// Unique identifier for the object.
    public var id: String
    /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in
    /// test mode.
    public var livemode: Bool
    /// A name for the secret that's unique within the scope.
    public var name: String
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: SecretObject
    /// The plaintext secret value to be stored.
    public var payload: String?
    @Expandable<SecretServiceResourceScope>
    public var scope: String?

    public enum CodingKeys: String, CodingKey {
        case created
        case deleted
        case expiresAt = "expires_at"
        case id
        case livemode
        case name
        case object
        case payload
        case scope
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum SecretObject: String, Codable, Sendable {
    case appsSecret = "apps.secret"
}

public struct SecretList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Secret]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

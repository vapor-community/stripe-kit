//
//  PhysicalBundleRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - PhysicalBundle Routes

public struct PhysicalBundleRoutes: Sendable {

    /// Retrieves a physical bundle object.
    public var retrieve: @Sendable (_ physicalBundle: String, _ expand: [String]?) async throws -> PhysicalBundle

    /// Returns a list of physical bundle objects. The objects are sorted in descending order by creation
    /// date, with the most recently created object appearing first.
    public var list: @Sendable (_ expand: [String]?) async throws -> PhysicalBundleList
}

extension PhysicalBundleRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        PhysicalBundleRoutes(
            retrieve: { physicalBundle, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/issuing/physical_bundles/\(physicalBundle)" : "/v1/issuing/physical_bundles/\(physicalBundle)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as PhysicalBundle
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/issuing/physical_bundles" : "/v1/issuing/physical_bundles?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as PhysicalBundleList
            }
        )
    }
}
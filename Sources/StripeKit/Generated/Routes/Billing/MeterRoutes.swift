//
//  MeterRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - Meter Routes

public struct MeterRoutes: Sendable {

    /// Creates a billing meter.
    public var create: @Sendable (_ params: MeterCreateParams) async throws -> Meter

    /// Retrieves a billing meter given an ID.
    public var retrieve: @Sendable (_ id: String, _ expand: [String]?) async throws -> Meter

    /// Updates a billing meter.
    public var update: @Sendable (_ id: String, _ params: MeterUpdateParams?) async throws -> Meter

    /// Retrieve a list of billing meters.
    public var list: @Sendable (_ expand: [String]?) async throws -> MeterList

    /// When a meter is deactivated, no more meter events will be accepted for this meter. You can’t attach
    /// a deactivated meter to a price.
    public var deactivate: @Sendable (_ id: String, _ params: MeterDeactivateParams?) async throws -> Meter

    /// When a meter is reactivated, events for this meter can be accepted and you can attach the meter to a
    /// price.
    public var reactivate: @Sendable (_ id: String, _ params: MeterReactivateParams?) async throws -> Meter
}

extension MeterRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        MeterRoutes(
            create: { params in
                let body = params.toFormBody()
                return try await handler.send(
                    method: .POST,
                    path: "/v1/billing/meters",
                    body: .bytes(ByteBuffer(string: body))
                ) as Meter
            },
            retrieve: { id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/billing/meters/\(id)" : "/v1/billing/meters/\(id)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as Meter
            },
            update: { id, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/billing/meters/\(id)",
                    body: .bytes(ByteBuffer(string: body))
                ) as Meter
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/billing/meters" : "/v1/billing/meters?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as MeterList
            },
            deactivate: { id, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/billing/meters/\(id)/deactivate",
                    body: .bytes(ByteBuffer(string: body))
                ) as Meter
            },
            reactivate: { id, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/billing/meters/\(id)/reactivate",
                    body: .bytes(ByteBuffer(string: body))
                ) as Meter
            }
        )
    }
}
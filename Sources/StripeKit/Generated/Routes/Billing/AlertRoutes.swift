//
//  AlertRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - Alert Routes

public struct AlertRoutes: Sendable {

    /// Creates a billing alert
    public var create: @Sendable (_ params: AlertCreateParams) async throws -> Alert

    /// Retrieves a billing alert given an ID
    public var retrieve: @Sendable (_ id: String, _ expand: [String]?) async throws -> Alert

    /// Lists billing active and inactive alerts
    public var list: @Sendable (_ expand: [String]?) async throws -> AlertList

    /// Reactivates this alert, allowing it to trigger again.
    public var activate: @Sendable (_ id: String, _ params: AlertActivateParams?) async throws -> Alert

    /// Archives this alert, removing it from the list view and APIs. This is non-reversible.
    public var archive: @Sendable (_ id: String, _ params: AlertArchiveParams?) async throws -> Alert

    /// Deactivates this alert, preventing it from triggering.
    public var deactivate: @Sendable (_ id: String, _ params: AlertDeactivateParams?) async throws -> Alert
}

extension AlertRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        AlertRoutes(
            create: { params in
                let body = params.toFormBody()
                return try await handler.send(
                    method: .POST,
                    path: "/v1/billing/alerts",
                    body: .bytes(ByteBuffer(string: body))
                ) as Alert
            },
            retrieve: { id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/billing/alerts/\(id)" : "/v1/billing/alerts/\(id)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as Alert
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/billing/alerts" : "/v1/billing/alerts?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as AlertList
            },
            activate: { id, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/billing/alerts/\(id)/activate",
                    body: .bytes(ByteBuffer(string: body))
                ) as Alert
            },
            archive: { id, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/billing/alerts/\(id)/archive",
                    body: .bytes(ByteBuffer(string: body))
                ) as Alert
            },
            deactivate: { id, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/billing/alerts/\(id)/deactivate",
                    body: .bytes(ByteBuffer(string: body))
                ) as Alert
            }
        )
    }
}
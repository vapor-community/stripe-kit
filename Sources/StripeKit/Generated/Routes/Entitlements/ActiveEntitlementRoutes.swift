//
//  ActiveEntitlementRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - ActiveEntitlement Routes

public struct ActiveEntitlementRoutes: Sendable {

    /// Retrieve an active entitlement
    public var retrieve: @Sendable (_ id: String, _ expand: [String]?) async throws -> ActiveEntitlement

    /// Retrieve a list of active entitlements for a customer
    public var list: @Sendable (_ expand: [String]?) async throws -> ActiveEntitlementList
}

extension ActiveEntitlementRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        ActiveEntitlementRoutes(
            retrieve: { id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/entitlements/active_entitlements/\(id)" : "/v1/entitlements/active_entitlements/\(id)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as ActiveEntitlement
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/entitlements/active_entitlements" : "/v1/entitlements/active_entitlements?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as ActiveEntitlementList
            }
        )
    }
}
//
//  SubscriptionItemRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - SubscriptionItem Routes

public struct SubscriptionItemRoutes: Sendable {

    /// Adds a new item to an existing subscription. No existing items will be changed or replaced.
    public var create: @Sendable (_ params: SubscriptionItemCreateParams) async throws -> SubscriptionItem

    /// Retrieves the subscription item with the given ID.
    public var retrieve: @Sendable (_ item: String, _ expand: [String]?) async throws -> SubscriptionItem

    /// Updates the plan or quantity of an item on a current subscription.
    public var update: @Sendable (_ item: String, _ params: SubscriptionItemUpdateParams?) async throws -> SubscriptionItem

    /// Returns a list of your subscription items for a given subscription.
    public var list: @Sendable (_ expand: [String]?) async throws -> SubscriptionItemList

    /// Deletes an item from the subscription. Removing a subscription item from a subscription will not
    /// cancel the subscription.
    public var delete: @Sendable (_ item: String, _ expand: [String]?) async throws -> SubscriptionItem
}

extension SubscriptionItemRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        SubscriptionItemRoutes(
            create: { params in
                let body = params.toFormBody()
                return try await handler.send(
                    method: .POST,
                    path: "/v1/subscription_items",
                    body: .bytes(ByteBuffer(string: body))
                ) as SubscriptionItem
            },
            retrieve: { item, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/subscription_items/\(item)" : "/v1/subscription_items/\(item)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as SubscriptionItem
            },
            update: { item, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/subscription_items/\(item)",
                    body: .bytes(ByteBuffer(string: body))
                ) as SubscriptionItem
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/subscription_items" : "/v1/subscription_items?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as SubscriptionItemList
            },
            delete: { item, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/subscription_items/\(item)" : "/v1/subscription_items/\(item)?\(query)"
                return try await handler.send(
                    method: .DELETE,
                    path: path
                ) as SubscriptionItem
            }
        )
    }
}
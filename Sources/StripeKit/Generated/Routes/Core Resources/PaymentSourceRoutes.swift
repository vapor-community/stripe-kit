//
//  PaymentSourceRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - PaymentSource Routes

public struct PaymentSourceRoutes: Sendable {

    /// When you create a new credit card, you must specify a customer or recipient on which to create it.
    /// If the card’s owner has no default card, then the new card will become the default. However, if the
    /// owner already has a default, then it will not change. To change the default, you should update the
    /// customer to have a new default_source.
    public var create: @Sendable (_ customer: String, _ params: PaymentSourceCreateParams) async throws -> PaymentSource

    /// Retrieve a specified source for a given customer.
    public var retrieve: @Sendable (_ customer: String, _ id: String, _ expand: [String]?) async throws -> PaymentSource

    /// Update a specified source for a given customer.
    public var update: @Sendable (_ customer: String, _ id: String, _ params: PaymentSourceUpdateParams?) async throws -> PaymentSource

    /// List sources for a specified customer.
    public var list: @Sendable (_ customer: String, _ expand: [String]?) async throws -> PaymentSourceList

    /// Delete a specified source for a given customer.
    public var delete: @Sendable (_ customer: String, _ id: String, _ expand: [String]?) async throws -> PaymentSource
}

extension PaymentSourceRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        PaymentSourceRoutes(
            create: { customer, params in
                let body = params.toFormBody()
                return try await handler.send(
                    method: .POST,
                    path: "/v1/customers/\(customer)/sources",
                    body: .bytes(ByteBuffer(string: body))
                ) as PaymentSource
            },
            retrieve: { customer, id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/customers/\(customer)/sources/\(id)" : "/v1/customers/\(customer)/sources/\(id)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as PaymentSource
            },
            update: { customer, id, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/customers/\(customer)/sources/\(id)",
                    body: .bytes(ByteBuffer(string: body))
                ) as PaymentSource
            },
            list: { customer, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/customers/\(customer)/sources" : "/v1/customers/\(customer)/sources?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as PaymentSourceList
            },
            delete: { customer, id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/customers/\(customer)/sources/\(id)" : "/v1/customers/\(customer)/sources/\(id)?\(query)"
                return try await handler.send(
                    method: .DELETE,
                    path: path
                ) as PaymentSource
            }
        )
    }
}
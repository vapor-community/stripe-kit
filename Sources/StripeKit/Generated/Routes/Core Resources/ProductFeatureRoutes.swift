//
//  ProductFeatureRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - ProductFeature Routes

public struct ProductFeatureRoutes: Sendable {

    /// Creates a product_feature, which represents a feature attachment to a product
    public var create: @Sendable (_ product: String, _ params: ProductFeatureCreateParams) async throws -> ProductFeature

    /// Retrieves a product_feature, which represents a feature attachment to a product
    public var retrieve: @Sendable (_ product: String, _ id: String, _ expand: [String]?) async throws -> ProductFeature

    /// Retrieve a list of features for a product
    public var list: @Sendable (_ product: String, _ expand: [String]?) async throws -> ProductFeatureList

    /// Deletes the feature attachment to a product
    public var delete: @Sendable (_ product: String, _ id: String, _ expand: [String]?) async throws -> ProductFeature
}

extension ProductFeatureRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        ProductFeatureRoutes(
            create: { product, params in
                let body = params.toFormBody()
                return try await handler.send(
                    method: .POST,
                    path: "/v1/products/\(product)/features",
                    body: .bytes(ByteBuffer(string: body))
                ) as ProductFeature
            },
            retrieve: { product, id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/products/\(product)/features/\(id)" : "/v1/products/\(product)/features/\(id)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as ProductFeature
            },
            list: { product, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/products/\(product)/features" : "/v1/products/\(product)/features?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as ProductFeatureList
            },
            delete: { product, id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/products/\(product)/features/\(id)" : "/v1/products/\(product)/features/\(id)?\(query)"
                return try await handler.send(
                    method: .DELETE,
                    path: path
                ) as ProductFeature
            }
        )
    }
}
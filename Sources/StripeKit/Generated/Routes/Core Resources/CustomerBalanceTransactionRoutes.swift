//
//  CustomerBalanceTransactionRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - CustomerBalanceTransaction Routes

public struct CustomerBalanceTransactionRoutes: Sendable {

    /// Creates an immutable transaction that updates the customer’s credit balance.
    public var create: @Sendable (_ customer: String, _ params: CustomerBalanceTransactionCreateParams) async throws -> CustomerBalanceTransaction

    /// Retrieves a specific customer balance transaction that updated the customer’s balances.
    public var retrieve: @Sendable (_ customer: String, _ transaction: String, _ expand: [String]?) async throws -> CustomerBalanceTransaction

    /// Most credit balance transaction fields are immutable, but you may update its description and
    /// metadata.
    public var update: @Sendable (_ customer: String, _ transaction: String, _ params: CustomerBalanceTransactionUpdateParams?) async throws -> CustomerBalanceTransaction

    /// Returns a list of transactions that updated the customer’s balances.
    public var list: @Sendable (_ customer: String, _ expand: [String]?) async throws -> CustomerBalanceTransactionList
}

extension CustomerBalanceTransactionRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        CustomerBalanceTransactionRoutes(
            create: { customer, params in
                let body = params.toFormBody()
                return try await handler.send(
                    method: .POST,
                    path: "/v1/customers/\(customer)/balance_transactions",
                    body: .bytes(ByteBuffer(string: body))
                ) as CustomerBalanceTransaction
            },
            retrieve: { customer, transaction, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/customers/\(customer)/balance_transactions/\(transaction)" : "/v1/customers/\(customer)/balance_transactions/\(transaction)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as CustomerBalanceTransaction
            },
            update: { customer, transaction, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/customers/\(customer)/balance_transactions/\(transaction)",
                    body: .bytes(ByteBuffer(string: body))
                ) as CustomerBalanceTransaction
            },
            list: { customer, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/customers/\(customer)/balance_transactions" : "/v1/customers/\(customer)/balance_transactions?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as CustomerBalanceTransactionList
            }
        )
    }
}
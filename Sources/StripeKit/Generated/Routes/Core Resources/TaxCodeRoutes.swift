//
//  TaxCodeRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - TaxCode Routes

public struct TaxCodeRoutes: Sendable {

    /// Retrieves the details of an existing tax code. Supply the unique tax code ID and Stripe will return
    /// the corresponding tax code information.
    public var retrieve: @Sendable (_ id: String, _ expand: [String]?) async throws -> TaxCode

    /// A list of all tax codes available to add to Products in order to allow specific tax calculations.
    public var list: @Sendable (_ expand: [String]?) async throws -> TaxCodeList
}

extension TaxCodeRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        TaxCodeRoutes(
            retrieve: { id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/tax_codes/\(id)" : "/v1/tax_codes/\(id)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as TaxCode
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/tax_codes" : "/v1/tax_codes?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as TaxCodeList
            }
        )
    }
}
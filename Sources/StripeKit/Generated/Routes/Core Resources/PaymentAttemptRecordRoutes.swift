//
//  PaymentAttemptRecordRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - PaymentAttemptRecord Routes

public struct PaymentAttemptRecordRoutes: Sendable {

    /// Retrieves a Payment Attempt Record with the given ID
    public var retrieve: @Sendable (_ id: String, _ expand: [String]?) async throws -> PaymentAttemptRecord

    /// List all the Payment Attempt Records attached to the specified Payment Record.
    public var list: @Sendable (_ expand: [String]?) async throws -> PaymentAttemptRecordList
}

extension PaymentAttemptRecordRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        PaymentAttemptRecordRoutes(
            retrieve: { id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/payment_attempt_records/\(id)" : "/v1/payment_attempt_records/\(id)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as PaymentAttemptRecord
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/payment_attempt_records" : "/v1/payment_attempt_records?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as PaymentAttemptRecordList
            }
        )
    }
}
//
//  ExchangeRateRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - ExchangeRate Routes

public struct ExchangeRateRoutes: Sendable {

    /// [Deprecated] The ExchangeRate APIs are deprecated. Please use the FX Quotes API instead. Retrieves
    /// the exchange rates from the given currency to every supported currency.
    public var retrieve: @Sendable (_ rateId: String, _ expand: [String]?) async throws -> ExchangeRate

    /// [Deprecated] The ExchangeRate APIs are deprecated. Please use the FX Quotes API instead. Returns a
    /// list of objects that contain the rates at which foreign currencies are converted to one another.
    /// Only shows the currencies for which Stripe supports.
    public var list: @Sendable (_ expand: [String]?) async throws -> ExchangeRateList
}

extension ExchangeRateRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        ExchangeRateRoutes(
            retrieve: { rateId, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/exchange_rates/\(rateId)" : "/v1/exchange_rates/\(rateId)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as ExchangeRate
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/exchange_rates" : "/v1/exchange_rates?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as ExchangeRateList
            }
        )
    }
}
//
//  TaxIdRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - TaxId Routes

public struct TaxIdRoutes: Sendable {

    /// Creates a new account or customer tax_id object.
    public var create: @Sendable (_ params: TaxIdCreateParams) async throws -> TaxId

    /// Retrieves an account or customer tax_id object.
    public var retrieve: @Sendable (_ id: String, _ expand: [String]?) async throws -> TaxId

    /// Creates a new tax_id object for a customer.
    public var update: @Sendable (_ customer: String, _ params: TaxIdUpdateParams) async throws -> TaxId

    /// Returns a list of tax IDs.
    public var list: @Sendable (_ expand: [String]?) async throws -> TaxIdList

    /// Deletes an existing account or customer tax_id object.
    public var delete: @Sendable (_ id: String, _ expand: [String]?) async throws -> TaxId
}

extension TaxIdRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        TaxIdRoutes(
            create: { params in
                let body = params.toFormBody()
                return try await handler.send(
                    method: .POST,
                    path: "/v1/tax_ids",
                    body: .bytes(ByteBuffer(string: body))
                ) as TaxId
            },
            retrieve: { id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/tax_ids/\(id)" : "/v1/tax_ids/\(id)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as TaxId
            },
            update: { customer, params in
                let body = params.toFormBody()
                return try await handler.send(
                    method: .POST,
                    path: "/v1/customers/\(customer)/tax_ids",
                    body: .bytes(ByteBuffer(string: body))
                ) as TaxId
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/tax_ids" : "/v1/tax_ids?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as TaxIdList
            },
            delete: { id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/tax_ids/\(id)" : "/v1/tax_ids/\(id)?\(query)"
                return try await handler.send(
                    method: .DELETE,
                    path: path
                ) as TaxId
            }
        )
    }
}
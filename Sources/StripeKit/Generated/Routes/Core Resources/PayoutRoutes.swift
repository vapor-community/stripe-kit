//
//  PayoutRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - Payout Routes

public struct PayoutRoutes: Sendable {

    /// To send funds to your own bank account, create a new payout object. Your Stripe balance must cover
    /// the payout amount. If it doesn’t, you receive an “Insufficient Funds” error. If your API key is in
    /// test mode, money won’t actually be sent, though every other action occurs as if you’re in live mode.
    /// If you create a manual payout on a Stripe account that uses multiple payment source types, you need
    /// to specify the source type balance that the payout draws from. The balance object details available
    /// and pending amounts by source type.
    public var create: @Sendable (_ params: PayoutCreateParams) async throws -> Payout

    /// Retrieves the details of an existing payout. Supply the unique payout ID from either a payout
    /// creation request or the payout list. Stripe returns the corresponding payout information.
    public var retrieve: @Sendable (_ payout: String, _ expand: [String]?) async throws -> Payout

    /// Updates the specified payout by setting the values of the parameters you pass. We don’t change
    /// parameters that you don’t provide. This request only accepts the metadata as arguments.
    public var update: @Sendable (_ payout: String, _ params: PayoutUpdateParams?) async throws -> Payout

    /// Returns a list of existing payouts sent to third-party bank accounts or payouts that Stripe sent to
    /// you. The payouts return in sorted order, with the most recently created payouts appearing first.
    public var list: @Sendable (_ expand: [String]?) async throws -> PayoutList

    /// You can cancel a previously created payout if its status is pending. Stripe refunds the funds to
    /// your available balance. You can’t cancel automatic Stripe payouts.
    public var cancel: @Sendable (_ payout: String, _ params: PayoutCancelParams?) async throws -> Payout

    /// Reverses a payout by debiting the destination bank account. At this time, you can only reverse
    /// payouts for connected accounts to US and Canadian bank accounts. If the payout is manual and in the
    /// pending status, use /v1/payouts/:id/cancel instead. By requesting a reversal through
    /// /v1/payouts/:id/reverse, you confirm that the authorized signatory of the selected bank account
    /// authorizes the debit on the bank account and that no other authorization is required.
    public var reverse: @Sendable (_ payout: String, _ params: PayoutReverseParams?) async throws -> Payout
}

extension PayoutRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        PayoutRoutes(
            create: { params in
                let body = params.toFormBody()
                return try await handler.send(
                    method: .POST,
                    path: "/v1/payouts",
                    body: .bytes(ByteBuffer(string: body))
                ) as Payout
            },
            retrieve: { payout, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/payouts/\(payout)" : "/v1/payouts/\(payout)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as Payout
            },
            update: { payout, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/payouts/\(payout)",
                    body: .bytes(ByteBuffer(string: body))
                ) as Payout
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/payouts" : "/v1/payouts?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as PayoutList
            },
            cancel: { payout, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/payouts/\(payout)/cancel",
                    body: .bytes(ByteBuffer(string: body))
                ) as Payout
            },
            reverse: { payout, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/payouts/\(payout)/reverse",
                    body: .bytes(ByteBuffer(string: body))
                ) as Payout
            }
        )
    }
}
//
//  CreditNoteRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - CreditNote Routes

public struct CreditNoteRoutes: Sendable {

    /// Issue a credit note to adjust the amount of a finalized invoice. A credit note will first reduce the
    /// invoice’s amount_remaining (and amount_due), but not below zero. This amount is indicated by the
    /// credit note’s pre_payment_amount. The excess amount is indicated by post_payment_amount, and it can
    /// result in any combination of the following: Refunds: create a new refund (using refund_amount) or
    /// link existing refunds (using refunds). Customer balance credit: credit the customer’s balance (using
    /// credit_amount) which will be automatically applied to their next invoice when it’s finalized.
    /// Outside of Stripe credit: record the amount that is or will be credited outside of Stripe (using
    /// out_of_band_amount). The sum of refunds, customer balance credits, and outside of Stripe credits
    /// must equal the post_payment_amount. You may issue multiple credit notes for an invoice. Each credit
    /// note may increment the invoice’s pre_payment_credit_notes_amount, post_payment_credit_notes_amount,
    /// or both, depending on the invoice’s amount_remaining at the time of credit note creation.
    public var create: @Sendable (_ params: CreditNoteCreateParams) async throws -> CreditNote

    /// Retrieves the credit note object with the given identifier.
    public var retrieve: @Sendable (_ id: String, _ expand: [String]?) async throws -> CreditNote

    /// Updates an existing credit note.
    public var update: @Sendable (_ id: String, _ params: CreditNoteUpdateParams?) async throws -> CreditNote

    /// Returns a list of credit notes.
    public var list: @Sendable (_ expand: [String]?) async throws -> CreditNoteList

    /// Get a preview of a credit note without creating it.
    public var listPreview: @Sendable (_ expand: [String]?) async throws -> CreditNote

    /// Marks a credit note as void. Learn more about voiding credit notes.
    public var void: @Sendable (_ id: String, _ params: CreditNoteVoidParams?) async throws -> CreditNote
}

extension CreditNoteRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        CreditNoteRoutes(
            create: { params in
                let body = params.toFormBody()
                return try await handler.send(
                    method: .POST,
                    path: "/v1/credit_notes",
                    body: .bytes(ByteBuffer(string: body))
                ) as CreditNote
            },
            retrieve: { id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/credit_notes/\(id)" : "/v1/credit_notes/\(id)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as CreditNote
            },
            update: { id, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/credit_notes/\(id)",
                    body: .bytes(ByteBuffer(string: body))
                ) as CreditNote
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/credit_notes" : "/v1/credit_notes?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as CreditNoteList
            },
            listPreview: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/credit_notes/preview" : "/v1/credit_notes/preview?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as CreditNote
            },
            void: { id, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/credit_notes/\(id)/void",
                    body: .bytes(ByteBuffer(string: body))
                ) as CreditNote
            }
        )
    }
}
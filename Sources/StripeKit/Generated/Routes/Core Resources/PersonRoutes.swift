//
//  PersonRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - Person Routes

public struct PersonRoutes: Sendable {

    /// Creates a new person.
    public var create: @Sendable (_ account: String, _ params: PersonCreateParams?) async throws -> Person

    /// Retrieves an existing person.
    public var retrieve: @Sendable (_ account: String, _ person: String, _ expand: [String]?) async throws -> Person

    /// Updates an existing person.
    public var update: @Sendable (_ account: String, _ person: String, _ params: PersonUpdateParams?) async throws -> Person

    /// Returns a list of people associated with the account’s legal entity. The people are returned sorted
    /// by creation date, with the most recent people appearing first.
    public var list: @Sendable (_ account: String, _ expand: [String]?) async throws -> PersonList

    /// Deletes an existing person’s relationship to the account’s legal entity. Any person with a
    /// relationship for an account can be deleted through the API, except if the person is the
    /// account_opener. If your integration is using the executive parameter, you cannot delete the only
    /// verified executive on file.
    public var delete: @Sendable (_ account: String, _ person: String, _ expand: [String]?) async throws -> Person
}

extension PersonRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        PersonRoutes(
            create: { account, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/accounts/\(account)/persons",
                    body: .bytes(ByteBuffer(string: body))
                ) as Person
            },
            retrieve: { account, person, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/accounts/\(account)/persons/\(person)" : "/v1/accounts/\(account)/persons/\(person)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as Person
            },
            update: { account, person, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/accounts/\(account)/persons/\(person)",
                    body: .bytes(ByteBuffer(string: body))
                ) as Person
            },
            list: { account, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/accounts/\(account)/persons" : "/v1/accounts/\(account)/persons?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as PersonList
            },
            delete: { account, person, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/accounts/\(account)/persons/\(person)" : "/v1/accounts/\(account)/persons/\(person)?\(query)"
                return try await handler.send(
                    method: .DELETE,
                    path: path
                ) as Person
            }
        )
    }
}
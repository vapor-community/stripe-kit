//
//  CountrySpecRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - CountrySpec Routes

public struct CountrySpecRoutes: Sendable {

    /// Returns a Country Spec for a given Country code.
    public var retrieve: @Sendable (_ country: String, _ expand: [String]?) async throws -> CountrySpec

    /// Lists all Country Spec objects available in the API.
    public var list: @Sendable (_ expand: [String]?) async throws -> CountrySpecList
}

extension CountrySpecRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        CountrySpecRoutes(
            retrieve: { country, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/country_specs/\(country)" : "/v1/country_specs/\(country)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as CountrySpec
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/country_specs" : "/v1/country_specs?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as CountrySpecList
            }
        )
    }
}
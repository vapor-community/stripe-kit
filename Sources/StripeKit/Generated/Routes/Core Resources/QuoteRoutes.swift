//
//  QuoteRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - Quote Routes

public struct QuoteRoutes: Sendable {

    /// A quote models prices and services for a customer. Default options for header, description, footer,
    /// and expires_at can be set in the dashboard via the quote template.
    public var create: @Sendable (_ params: QuoteCreateParams?) async throws -> Quote

    /// Retrieves the quote with the given ID.
    public var retrieve: @Sendable (_ quote: String, _ expand: [String]?) async throws -> Quote

    /// A quote models prices and services for a customer.
    public var update: @Sendable (_ quote: String, _ params: QuoteUpdateParams?) async throws -> Quote

    /// Returns a list of your quotes.
    public var list: @Sendable (_ expand: [String]?) async throws -> QuoteList

    /// Accepts the specified quote.
    public var accept: @Sendable (_ quote: String, _ params: QuoteAcceptParams?) async throws -> Quote

    /// Cancels the quote.
    public var cancel: @Sendable (_ quote: String, _ params: QuoteCancelParams?) async throws -> Quote

    /// Finalizes the quote.
    public var finalize: @Sendable (_ quote: String, _ params: QuoteFinalizeParams?) async throws -> Quote
}

extension QuoteRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        QuoteRoutes(
            create: { params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/quotes",
                    body: .bytes(ByteBuffer(string: body))
                ) as Quote
            },
            retrieve: { quote, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/quotes/\(quote)" : "/v1/quotes/\(quote)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as Quote
            },
            update: { quote, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/quotes/\(quote)",
                    body: .bytes(ByteBuffer(string: body))
                ) as Quote
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/quotes" : "/v1/quotes?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as QuoteList
            },
            accept: { quote, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/quotes/\(quote)/accept",
                    body: .bytes(ByteBuffer(string: body))
                ) as Quote
            },
            cancel: { quote, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/quotes/\(quote)/cancel",
                    body: .bytes(ByteBuffer(string: body))
                ) as Quote
            },
            finalize: { quote, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/quotes/\(quote)/finalize",
                    body: .bytes(ByteBuffer(string: body))
                ) as Quote
            }
        )
    }
}
//
//  BalanceRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - Balance Routes

public struct BalanceRoutes: Sendable {

    /// Retrieves the current account balance, based on the authentication that was used to make the
    /// request. For a sample request, see Accounting for negative balances.
    public var list: @Sendable (_ expand: [String]?) async throws -> BalanceList
}

extension BalanceRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        BalanceRoutes(
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/balance" : "/v1/balance?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as BalanceList
            }
        )
    }
}
//
//  FeeRefundRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - FeeRefund Routes

public struct FeeRefundRoutes: Sendable {

    /// Refunds an application fee that has previously been collected but not yet refunded. Funds will be
    /// refunded to the Stripe account from which the fee was originally collected. You can optionally
    /// refund only part of an application fee. You can do so multiple times, until the entire fee has been
    /// refunded. Once entirely refunded, an application fee can’t be refunded again. This method will raise
    /// an error when called on an already-refunded application fee, or when trying to refund more money
    /// than is left on an application fee.
    public var create: @Sendable (_ id: String, _ params: FeeRefundCreateParams?) async throws -> FeeRefund

    /// By default, you can see the 10 most recent refunds stored directly on the application fee object,
    /// but you can also retrieve details about a specific refund stored on the application fee.
    public var retrieve: @Sendable (_ fee: String, _ id: String, _ expand: [String]?) async throws -> FeeRefund

    /// Updates the specified application fee refund by setting the values of the parameters passed. Any
    /// parameters not provided will be left unchanged. This request only accepts metadata as an argument.
    public var update: @Sendable (_ fee: String, _ id: String, _ params: FeeRefundUpdateParams?) async throws -> FeeRefund

    /// You can see a list of the refunds belonging to a specific application fee. Note that the 10 most
    /// recent refunds are always available by default on the application fee object. If you need more than
    /// those 10, you can use this API method and the limit and starting_after parameters to page through
    /// additional refunds.
    public var list: @Sendable (_ id: String, _ expand: [String]?) async throws -> FeeRefundList
}

extension FeeRefundRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        FeeRefundRoutes(
            create: { id, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/application_fees/\(id)/refunds",
                    body: .bytes(ByteBuffer(string: body))
                ) as FeeRefund
            },
            retrieve: { fee, id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/application_fees/\(fee)/refunds/\(id)" : "/v1/application_fees/\(fee)/refunds/\(id)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as FeeRefund
            },
            update: { fee, id, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/application_fees/\(fee)/refunds/\(id)",
                    body: .bytes(ByteBuffer(string: body))
                ) as FeeRefund
            },
            list: { id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/application_fees/\(id)/refunds" : "/v1/application_fees/\(id)/refunds?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as FeeRefundList
            }
        )
    }
}
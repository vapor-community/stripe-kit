//
//  TestClockRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - TestClock Routes

public struct TestClockRoutes: Sendable {

    /// Creates a new test clock that can be attached to new customers and quotes.
    public var create: @Sendable (_ params: TestClockCreateParams) async throws -> TestClock

    /// Retrieves a test clock.
    public var retrieve: @Sendable (_ testClock: String, _ expand: [String]?) async throws -> TestClock

    /// Returns a list of your test clocks.
    public var list: @Sendable (_ expand: [String]?) async throws -> TestClockList

    /// Deletes a test clock.
    public var delete: @Sendable (_ testClock: String, _ expand: [String]?) async throws -> TestClock

    /// Starts advancing a test clock to a specified time in the future. Advancement is done when status
    /// changes to Ready.
    public var advance: @Sendable (_ testClock: String, _ params: TestClockAdvanceParams) async throws -> TestClock
}

extension TestClockRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        TestClockRoutes(
            create: { params in
                let body = params.toFormBody()
                return try await handler.send(
                    method: .POST,
                    path: "/v1/test_helpers/test_clocks",
                    body: .bytes(ByteBuffer(string: body))
                ) as TestClock
            },
            retrieve: { testClock, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/test_helpers/test_clocks/\(testClock)" : "/v1/test_helpers/test_clocks/\(testClock)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as TestClock
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/test_helpers/test_clocks" : "/v1/test_helpers/test_clocks?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as TestClockList
            },
            delete: { testClock, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/test_helpers/test_clocks/\(testClock)" : "/v1/test_helpers/test_clocks/\(testClock)?\(query)"
                return try await handler.send(
                    method: .DELETE,
                    path: path
                ) as TestClock
            },
            advance: { testClock, params in
                let body = params.toFormBody()
                return try await handler.send(
                    method: .POST,
                    path: "/v1/test_helpers/test_clocks/\(testClock)/advance",
                    body: .bytes(ByteBuffer(string: body))
                ) as TestClock
            }
        )
    }
}
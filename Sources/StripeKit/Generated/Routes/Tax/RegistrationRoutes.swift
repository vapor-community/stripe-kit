//
//  RegistrationRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - Registration Routes

public struct RegistrationRoutes: Sendable {

    /// Creates a new Tax Registration object.
    public var create: @Sendable (_ params: RegistrationCreateParams) async throws -> Registration

    /// Returns a Tax Registration object.
    public var retrieve: @Sendable (_ id: String, _ expand: [String]?) async throws -> Registration

    /// Updates an existing Tax Registration object. A registration cannot be deleted after it has been
    /// created. If you wish to end a registration you may do so by setting expires_at.
    public var update: @Sendable (_ id: String, _ params: RegistrationUpdateParams?) async throws -> Registration

    /// Returns a list of Tax Registration objects.
    public var list: @Sendable (_ expand: [String]?) async throws -> RegistrationList
}

extension RegistrationRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        RegistrationRoutes(
            create: { params in
                let body = params.toFormBody()
                return try await handler.send(
                    method: .POST,
                    path: "/v1/tax/registrations",
                    body: .bytes(ByteBuffer(string: body))
                ) as Registration
            },
            retrieve: { id, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/tax/registrations/\(id)" : "/v1/tax/registrations/\(id)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as Registration
            },
            update: { id, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/tax/registrations/\(id)",
                    body: .bytes(ByteBuffer(string: body))
                ) as Registration
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/tax/registrations" : "/v1/tax/registrations?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as RegistrationList
            }
        )
    }
}
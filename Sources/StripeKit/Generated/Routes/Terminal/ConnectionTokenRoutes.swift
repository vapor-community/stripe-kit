//
//  ConnectionTokenRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - ConnectionToken Routes

public struct ConnectionTokenRoutes: Sendable {

    /// To connect to a reader the Stripe Terminal SDK needs to retrieve a short-lived connection token from
    /// Stripe, proxied through your server. On your backend, add an endpoint that creates and returns a
    /// connection token.
    public var create: @Sendable (_ params: ConnectionTokenCreateParams?) async throws -> ConnectionToken
}

extension ConnectionTokenRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        ConnectionTokenRoutes(
            create: { params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/terminal/connection_tokens",
                    body: .bytes(ByteBuffer(string: body))
                ) as ConnectionToken
            }
        )
    }
}
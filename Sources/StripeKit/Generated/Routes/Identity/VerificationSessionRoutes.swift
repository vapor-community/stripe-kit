//
//  VerificationSessionRoutes.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation
import NIO
import NIOHTTP1

// MARK: - VerificationSession Routes

public struct VerificationSessionRoutes: Sendable {

    /// Creates a VerificationSession object. After the VerificationSession is created, display a
    /// verification modal using the session client_secret or send your users to the session’s url. If your
    /// API key is in test mode, verification checks won’t actually process, though everything else will
    /// occur as if in live mode. Related guide: Verify your users’ identity documents
    public var create: @Sendable (_ params: VerificationSessionCreateParams?) async throws -> VerificationSession

    /// Retrieves the details of a VerificationSession that was previously created. When the session status
    /// is requires_input, you can use this method to retrieve a valid client_secret or url to allow
    /// re-submission.
    public var retrieve: @Sendable (_ session: String, _ expand: [String]?) async throws -> VerificationSession

    /// Updates a VerificationSession object. When the session status is requires_input, you can use this
    /// method to update the verification check and options.
    public var update: @Sendable (_ session: String, _ params: VerificationSessionUpdateParams?) async throws -> VerificationSession

    /// Returns a list of VerificationSessions
    public var list: @Sendable (_ expand: [String]?) async throws -> VerificationSessionList

    /// A VerificationSession object can be canceled when it is in requires_input status. Once canceled,
    /// future submission attempts are disabled. This cannot be undone. Learn more.
    public var cancel: @Sendable (_ session: String, _ params: VerificationSessionCancelParams?) async throws -> VerificationSession

    /// Redact a VerificationSession to remove all collected information from Stripe. This will redact the
    /// VerificationSession and all objects related to it, including VerificationReports, Events, request
    /// logs, etc. A VerificationSession object can be redacted when it is in requires_input or verified
    /// status. Redacting a VerificationSession in requires_action state will automatically cancel it. The
    /// redaction process may take up to four days. When the redaction process is in progress, the
    /// VerificationSession’s redaction.status field will be set to processing; when the process is
    /// finished, it will change to redacted and an identity.verification_session.redacted event will be
    /// emitted. Redaction is irreversible. Redacted objects are still accessible in the Stripe API, but all
    /// the fields that contain personal data will be replaced by the string [redacted] or a similar
    /// placeholder. The metadata field will also be erased. Redacted objects cannot be updated or used for
    /// any purpose. Learn more.
    public var redact: @Sendable (_ session: String, _ params: VerificationSessionRedactParams?) async throws -> VerificationSession
}

extension VerificationSessionRoutes {
    static func live(handler: StripeAPIHandler) -> Self {
        VerificationSessionRoutes(
            create: { params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/identity/verification_sessions",
                    body: .bytes(ByteBuffer(string: body))
                ) as VerificationSession
            },
            retrieve: { session, expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/identity/verification_sessions/\(session)" : "/v1/identity/verification_sessions/\(session)?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as VerificationSession
            },
            update: { session, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/identity/verification_sessions/\(session)",
                    body: .bytes(ByteBuffer(string: body))
                ) as VerificationSession
            },
            list: { expand in
                var query = ""
                if let expand { query = expand.map { "expand[]=\($0)" }.joined(separator: "&") }
                let path = query.isEmpty ? "/v1/identity/verification_sessions" : "/v1/identity/verification_sessions?\(query)"
                return try await handler.send(
                    method: .GET,
                    path: path
                ) as VerificationSessionList
            },
            cancel: { session, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/identity/verification_sessions/\(session)/cancel",
                    body: .bytes(ByteBuffer(string: body))
                ) as VerificationSession
            },
            redact: { session, params in
                let body = params?.toFormBody() ?? ""
                return try await handler.send(
                    method: .POST,
                    path: "/v1/identity/verification_sessions/\(session)/redact",
                    body: .bytes(ByteBuffer(string: body))
                ) as VerificationSession
            }
        )
    }
}
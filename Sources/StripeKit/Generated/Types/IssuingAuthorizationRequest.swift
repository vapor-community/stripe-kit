//
//  IssuingAuthorizationRequest.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

public struct IssuingAuthorizationRequest: Codable, Sendable {
    /// The `pending_request.amount` at the time of the request, presented in your card's currency and in
    /// the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). Stripe held this
    /// amount from your account to fund the authorization if the request was approved.
    public var amount: Int
    /// Detailed breakdown of amount components. These amounts are denominated in `currency` and in the
    /// [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
    @Expandable<IssuingAuthorizationAmountDetails>
    public var amountDetails: String?
    /// Whether this request was approved.
    public var approved: Bool
    /// A code created by Stripe which is shared with the merchant to validate the authorization. This field
    /// will be populated if the authorization message was approved. The code typically starts with the
    /// letter "S", followed by a six-digit number. For example, "S498162". Please note that the code is not
    /// guaranteed to be unique across authorizations.
    public var authorizationCode: String?
    /// Time at which the object was created. Measured in seconds since the Unix epoch.
    public var created: Int
    /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
    /// Must be a [supported currency](https://stripe.com/docs/currencies).
    public var currency: String
    /// The `pending_request.merchant_amount` at the time of the request, presented in the
    /// `merchant_currency` and in the [smallest currency
    /// unit](https://docs.stripe.com/currencies#zero-decimal).
    public var merchantAmount: Int
    /// The currency that was collected by the merchant and presented to the cardholder for the
    /// authorization. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html),
    /// in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
    public var merchantCurrency: String
    /// The card network's estimate of the likelihood that an authorization is fraudulent. Takes on values
    /// between 1 and 99.
    public var networkRiskScore: Int?
    /// When an authorization is approved or declined by you or by Stripe, this field provides additional
    /// detail on the reason for the outcome.
    public var reason: IssuingAuthorizationRequestReason
    /// If the `request_history.reason` is `webhook_error` because the direct webhook response is invalid
    /// (for example, parsing errors or missing parameters), we surface a more detailed error message via
    /// this field.
    public var reasonMessage: String?
    /// Time when the card network received an authorization request from the acquirer in UTC. Referred to
    /// by networks as transmission time.
    public var requestedAt: Int?

    public enum CodingKeys: String, CodingKey {
        case amount
        case amountDetails = "amount_details"
        case approved
        case authorizationCode = "authorization_code"
        case created
        case currency
        case merchantAmount = "merchant_amount"
        case merchantCurrency = "merchant_currency"
        case networkRiskScore = "network_risk_score"
        case reason
        case reasonMessage = "reason_message"
        case requestedAt = "requested_at"
    }
}

/// When an authorization is approved or declined by you or by Stripe, this field provides additional
/// detail on the reason for the outcome.
public enum IssuingAuthorizationRequestReason: String, Codable, Sendable {
    case accountDisabled = "account_disabled"
    case cardActive = "card_active"
    case cardCanceled = "card_canceled"
    case cardExpired = "card_expired"
    case cardInactive = "card_inactive"
    case cardholderBlocked = "cardholder_blocked"
    case cardholderInactive = "cardholder_inactive"
    case cardholderVerificationRequired = "cardholder_verification_required"
    case insecureAuthorizationMethod = "insecure_authorization_method"
    case insufficientFunds = "insufficient_funds"
    case networkFallback = "network_fallback"
    case notAllowed = "not_allowed"
    case pinBlocked = "pin_blocked"
    case spendingControls = "spending_controls"
    case suspectedFraud = "suspected_fraud"
    case verificationFailed = "verification_failed"
    case webhookApproved = "webhook_approved"
    case webhookDeclined = "webhook_declined"
    case webhookError = "webhook_error"
    case webhookTimeout = "webhook_timeout"
}

public struct IssuingAuthorizationRequestList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [IssuingAuthorizationRequest]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

//
//  Discount.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

/// A discount represents the actual application of a [coupon](https://api.stripe.com#coupons) or
/// [promotion code](https://api.stripe.com#promotion_codes). It contains information about when the
/// discount began, when it will end, and what it is applied to. Related guide: [Applying discounts to
/// subscriptions](https://docs.stripe.com/billing/subscriptions/discounts)
public struct Discount: Codable, Sendable {
    /// The Checkout session that this coupon is applied to, if it is applied to a particular session in
    /// payment mode. Will not be present for subscription mode.
    public var checkoutSession: String?
    /// The ID of the customer associated with this discount.
    @Expandable<Customer>
    public var customer: String?
    /// The ID of the account representing the customer associated with this discount.
    public var customerAccount: String?
    /// If the coupon has a duration of `repeating`, the date that this discount will end. If the coupon has
    /// a duration of `once` or `forever`, this attribute will be null.
    public var end: Int?
    /// The ID of the discount object. Discounts cannot be fetched by ID. Use `expand[]=discounts` in API
    /// calls to expand discount IDs in an array.
    public var id: String
    /// The invoice that the discount's coupon was applied to, if it was applied directly to a particular
    /// invoice.
    public var invoice: String?
    /// The invoice item `id` (or invoice line item `id` for invoice line items of type='subscription') that
    /// the discount's coupon was applied to, if it was applied directly to a particular invoice item or
    /// invoice line item.
    public var invoiceItem: String?
    /// String representing the object's type. Objects of the same type share the same value.
    public var object: DiscountObject
    /// The promotion code applied to create this discount.
    @Expandable<PromotionCode>
    public var promotionCode: String?
    @Expandable<DiscountSource>
    public var source: String?
    /// Date that the coupon was applied.
    public var start: Int
    /// The subscription that this coupon is applied to, if it is applied to a particular subscription.
    public var subscription: String?
    /// The subscription item that this coupon is applied to, if it is applied to a particular subscription
    /// item.
    public var subscriptionItem: String?

    public enum CodingKeys: String, CodingKey {
        case checkoutSession = "checkout_session"
        case customer
        case customerAccount = "customer_account"
        case end
        case id
        case invoice
        case invoiceItem = "invoice_item"
        case object
        case promotionCode = "promotion_code"
        case source
        case start
        case subscription
        case subscriptionItem = "subscription_item"
    }
}

/// String representing the object's type. Objects of the same type share the same value.
public enum DiscountObject: String, Codable, Sendable {
    case discount
}

public struct DiscountList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [Discount]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

//
//  PayoutsTraceId.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

public struct PayoutsTraceId: Codable, Sendable {
    /// Possible values are `pending`, `supported`, and `unsupported`. When `payout.status` is `pending` or
    /// `in_transit`, this will be `pending`. When the payout transitions to `paid`, `failed`, or
    /// `canceled`, this status will become `supported` or `unsupported` shortly after in most cases. In
    /// some cases, this may appear as `pending` for up to 10 days after `arrival_date` until transitioning
    /// to `supported` or `unsupported`.
    public var status: String
    /// The trace ID value if `trace_id.status` is `supported`, otherwise `nil`.
    public var value: String?
}

public struct PayoutsTraceIdList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [PayoutsTraceId]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

//
//  PortalSubscriptionUpdate.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

public struct PortalSubscriptionUpdate: Codable, Sendable {
    /// Determines the value to use for the billing cycle anchor on subscription updates. Valid values are
    /// `now` or `unchanged`, and the default value is `unchanged`. Setting the value to `now` resets the
    /// subscription's billing cycle anchor to the current time (in UTC). For more information, see the
    /// billing cycle [documentation](https://docs.stripe.com/billing/subscriptions/billing-cycle).
    public var billingCycleAnchor: PortalSubscriptionUpdateBillingCycleAnchor?
    /// The types of subscription updates that are supported for items listed in the `products` attribute.
    /// When empty, subscriptions are not updateable.
    public var defaultAllowedUpdates: [String]
    /// Whether the feature is enabled.
    public var enabled: Bool
    /// The list of up to 10 products that support subscription updates.
    @ExpandableCollection<PortalSubscriptionUpdateProduct>
    public var products: [String]?
    /// Determines how to handle prorations resulting from subscription updates. Valid values are `none`,
    /// `create_prorations`, and `always_invoice`. Defaults to a value of `none` if you don't set it during
    /// creation.
    public var prorationBehavior: PortalSubscriptionUpdateProrationBehavior
    @Expandable<PortalResourceScheduleUpdateAtPeriodEnd>
    public var scheduleAtPeriodEnd: String?
    /// Determines how handle updates to trialing subscriptions. Valid values are `end_trial` and
    /// `continue_trial`. Defaults to a value of `end_trial` if you don't set it during creation.
    public var trialUpdateBehavior: PortalSubscriptionUpdateTrialUpdateBehavior

    public enum CodingKeys: String, CodingKey {
        case billingCycleAnchor = "billing_cycle_anchor"
        case defaultAllowedUpdates = "default_allowed_updates"
        case enabled
        case products
        case prorationBehavior = "proration_behavior"
        case scheduleAtPeriodEnd = "schedule_at_period_end"
        case trialUpdateBehavior = "trial_update_behavior"
    }
}

/// Determines the value to use for the billing cycle anchor on subscription updates. Valid values are
/// `now` or `unchanged`, and the default value is `unchanged`. Setting the value to `now` resets the
/// subscription's billing cycle anchor to the current time (in UTC). For more information, see the
/// billing cycle [documentation](https://docs.stripe.com/billing/subscriptions/billing-cycle).
public enum PortalSubscriptionUpdateBillingCycleAnchor: String, Codable, Sendable {
    case now
    case unchanged
}

/// Determines how to handle prorations resulting from subscription updates. Valid values are `none`,
/// `create_prorations`, and `always_invoice`. Defaults to a value of `none` if you don't set it during
/// creation.
public enum PortalSubscriptionUpdateProrationBehavior: String, Codable, Sendable {
    case alwaysInvoice = "always_invoice"
    case createProrations = "create_prorations"
    case none
}

/// Determines how handle updates to trialing subscriptions. Valid values are `end_trial` and
/// `continue_trial`. Defaults to a value of `end_trial` if you don't set it during creation.
public enum PortalSubscriptionUpdateTrialUpdateBehavior: String, Codable, Sendable {
    case continueTrial = "continue_trial"
    case endTrial = "end_trial"
}

public struct PortalSubscriptionUpdateList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [PortalSubscriptionUpdate]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}

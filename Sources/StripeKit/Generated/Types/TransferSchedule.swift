//
//  TransferSchedule.swift
//  StripeKit
//
//  Generated by stripe-kit-codegen
//

import Foundation

public struct TransferSchedule: Codable, Sendable {
    /// The number of days charges for the account will be held before being paid out.
    public var delayDays: Int
    /// How frequently funds will be paid out. One of `manual` (payouts only created via API call), `daily`,
    /// `weekly`, or `monthly`.
    public var interval: String
    /// The day of the month funds will be paid out. Only shown if `interval` is monthly. Payouts scheduled
    /// between the 29th and 31st of the month are sent on the last day of shorter months.
    public var monthlyAnchor: Int?
    /// The days of the month funds will be paid out. Only shown if `interval` is monthly. Payouts scheduled
    /// between the 29th and 31st of the month are sent on the last day of shorter months.
    public var monthlyPayoutDays: [Int]?
    /// The day of the week funds will be paid out, of the style 'monday', 'tuesday', etc. Only shown if
    /// `interval` is weekly.
    public var weeklyAnchor: String?
    /// The days of the week when available funds are paid out, specified as an array, for example,
    /// [`monday`, `tuesday`]. Only shown if `interval` is weekly.
    public var weeklyPayoutDays: [String]?

    public enum CodingKeys: String, CodingKey {
        case delayDays = "delay_days"
        case interval
        case monthlyAnchor = "monthly_anchor"
        case monthlyPayoutDays = "monthly_payout_days"
        case weeklyAnchor = "weekly_anchor"
        case weeklyPayoutDays = "weekly_payout_days"
    }
}

public struct TransferScheduleList: Codable, Sendable {
    public var object: String
    public var hasMore: Bool?
    public var url: String?
    public var data: [TransferSchedule]?

    public enum CodingKeys: String, CodingKey {
        case object
        case hasMore = "has_more"
        case url
        case data
    }
}
